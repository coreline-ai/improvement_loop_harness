id: bad-absolute-path
title: Bad absolute path
objective: This fixture should fail due to an absolute write scope path.
write_scope:
  allowed:
    - /src/features/auth/
required_evidence:
  - adds_regression_test
