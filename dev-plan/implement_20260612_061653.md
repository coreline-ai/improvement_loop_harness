# implement_20260612_061653.md

작성 일시: `2026-06-12 06:16:53 KST`
최종 수정 일시: `2026-06-12 18:50 KST` — Review 5 M2 반영: Phase 본문 체크박스 67개를 실제 완료 상태와 정합화. 기존 Review 3 반영 코드·문서·로컬 게이트·원격 CI 통과 근거는 run 27389500328 및 최신 run 27406109586.

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

[EXPERT_REVIEW_3_20260612.md](../docs/EXPERT_REVIEW_3_20260612.md)(구현 완료 검증)의 발견 사항을 전부 반영한다. 핵심은 G1 — **검증 커널과 서버를 잇는 production 조립**(runKernel 기반 LoopRunner + 영속 배선 + 기동 진입점)을 완성해 "테스트 그린" 상태를 "가동 가능" 상태로 만드는 것이다. 나머지는 PrismaStore 검증(G2), seq 원자화(G5), 스펙 개정 일괄(G3·L1·L2·L3·L4), CI·smoke 연결(G4·G6)이다.

## 개발 범위

- Phase 1: 서버 production runner — runKernel 실행·DB 영속 배선 (G1 전반부)
- Phase 2: 서버 부트스트랩 main.ts·기동·조립 e2e (G1 마감)
- Phase 3: Store 계약 테스트 + LoopEvent seq 원자화 (G2·G5)
- Phase 4: 스펙 개정 일괄 + baseline 코드 정리 (G3·L1·L2·L3·L4)
- Phase 5: CI 워크플로 + Playwright smoke 연결 (G4·G6)

## 제외 범위

- 새 기능 추가 없음 — EXPERT_REVIEW_3에 없는 리팩터링·기능 확장 금지
- 멀티 유저 RBAC, 컨테이너 격리, Windows 지원 (기존 제외 유지)
- LLM critic runner, `vibeloop init`, discovery 소스 확장 (기존 후속 과제 유지)
- 자동 merge (영구 금지)

## 참조 문서

- [docs/EXPERT_REVIEW_3_20260612.md](../docs/EXPERT_REVIEW_3_20260612.md) — 이번 워크스트림의 발견 사항 원본 (G1~G6, L1~L5)
- [docs/API_SPEC.md](../docs/API_SPEC.md) / [docs/DB_SCHEMA.md](../docs/DB_SCHEMA.md) / [docs/SECURITY_MODEL.md](../docs/SECURITY_MODEL.md) / [docs/EVAL_ENGINE_SPEC.md](../docs/EVAL_ENGINE_SPEC.md) — Phase 4 개정 대상
- [docs/AUTONOMOUS_LOOP_SPEC.md](../docs/AUTONOMOUS_LOOP_SPEC.md) — orchestrator runner 주입 계약
- [docs/ARTIFACT_SCHEMA.md](../docs/ARTIFACT_SCHEMA.md) — runner가 읽는 커널 산출물 위치

## 이전 개발 계획

- [implement_20260610_223129.md](./implement_20260610_223129.md) — Phase 1~17 (MVP-0~4) 완료. 본 워크스트림은 그 구현에 대한 3차 검토 반영이다.

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- Phase 완료마다 단일 커밋(`fix(review3-phase-N): ...`)을 만든다.
- 수동 검증은 기록(명령 출력 로그·결과 ref)을 이 문서의 해당 Phase "이슈 및 수정"에 남긴다 (L5 규칙).
- `docs/`·`schemas/`가 source of truth — 코드와 충돌 시 스펙 개정 절차 선행.

## Phase 상태 요약

- [x] Phase 1 완료 — 서버 production runner·영속 배선
- [x] Phase 2 완료 — 부트스트랩·기동·조립 e2e
- [x] Phase 3 완료 — Store 계약 테스트·seq 원자화
- [x] Phase 4 완료 — 스펙 개정·baseline 정리
- [x] Phase 5 완료 — CI·smoke 연결(워크플로 구성 + 로컬 smoke 통과)

## 반영/검증 기록 (2026-06-12 10:59 KST)

| 항목 | 결과 | 기록 |
|---|---:|---|
| Prisma Client 생성 | 통과 | `pnpm exec prisma generate` |
| TypeScript | 통과 | `pnpm typecheck` |
| Lint | 통과 | `pnpm lint` |
| Unit/contract | 통과 | `pnpm test` — 15 files / 115 tests |
| E2E fixture | 통과 | `pnpm test:e2e` — 1 file / 16 tests |
| 전체 build | 통과 | `pnpm build` |
| Web build | 통과 | `pnpm build:web` |
| Playwright smoke | 통과 | `pnpm test:smoke` — 4 tests |
| 공백/patch hygiene | 통과 | `git diff --check` |
| 원격 CI 전체 | 통과 | ubuntu verify, macOS verify, prisma-store, web smoke 전부 green: https://github.com/coreline-ai/improvement_loop_harness/actions/runs/27389500328 |
| PrismaStore 실제 DB 계약 테스트 | 통과 | GitHub Actions `prisma-store` job에서 Postgres service + `TEST_DATABASE_URL`로 실행 완료: https://github.com/coreline-ai/improvement_loop_harness/actions/runs/27389500328 |

검증 중 `runCommand`의 execa 기반 timeout 경로가 `gate-timeout`/process-group kill 시 간헐적으로 hang되는 문제가 재현되어, `packages/shared/src/exec.ts`를 Node `spawn` 기반 직접 실행기로 교체하고 timeout 시 process group SIGTERM→SIGKILL→safety resolve를 보강했다. 수정 후 unit/e2e/smoke와 원격 CI 전체가 통과했다.

## Phase 1. 서버 production runner — 커널 실행·영속 배선 (G1)

### 목표
- `runKernel`을 호출해 산출물을 DB rows로 영속하는 `LoopRunner` 구현체를 만든다. dead schema(GateRun·AgentRun·WorkspaceRun)를 실사용으로 전환하고, 실운영에서 reports API·웹 화면이 빈 데이터가 되는 문제를 해소한다.

### 구현 태스크
- [x] `apps/server/src/types.ts` Store 인터페이스 확장 — `createGateRun`/`listGateRuns(loopRunId)`, `createAgentRun`, `createWorkspaceRun`(+ 조회 최소 1종) 추가, `GateRunRecord`·`AgentRunRecord`·`WorkspaceRunRecord` 타입 정의 (prisma 모델 필드와 1:1)
- [x] `memory-store.ts`·`prisma-store.ts`에 위 메서드 구현 (두 구현 동일 시그니처)
- [x] `apps/server/src/runner.ts` — `createKernelLoopRunner({store, dataDir, defaultAgentSpec})`: TaskRecord.taskYaml을 임시 디렉터리에 task.yaml로 직렬화 → project.localPath + evalConfigPath로 eval.yaml 경로 해석 → `runKernel({repoPath, taskFile, evalFile, dataDir, agentSpec, loopId})` 호출
- [x] runner 영속 배선 — 커널 산출물을 읽어 rows 생성: `reports/eval-report.json`→`createReport(type:'eval', reportJson, artifactRef)`, `reports/gate-report.json`→gate별 `createGateRun`, `manifest.json` checksums→`createArtifact`(kind/path/sha256/sizeBytes), agent 실행 메타→`createAgentRun`, worktree 메타→`createWorkspaceRun`
- [x] agent spec 결정 경로 — 서버 env `VIBELOOP_AGENT_SPEC` 기본값 + `POST /loops` body의 `agent_spec` 옵션 수용(없으면 기본값, requestHash에 포함됨 확인). API_SPEC 개정은 Phase 4에서
- [x] 상태 매핑 — RunKernelResult→`LoopRunnerResult{status, decision, artifactRoot, tokenUsageTotal}`, decision별 loop status는 기존 decision 상태명 그대로
- [x] runner 단위 테스트 — 임시 git repo + mock agent 시나리오로 1회 실행 → rows 카운트·내용 검증

### 자체 테스트
- [x] runner 1회 실행 후: EvalReport 1건·GateRun = gate 수·Artifact ≥ 1·AgentRun 1·WorkspaceRun 1 rows 존재
- [x] `GET /api/loops/:id/reports`가 실행 후 비어 있지 않고 decision 일치
- [x] runner 내부 예외 → loop status `failed` + `loop.failed` 이벤트 + 부분 영속 없음(또는 일관 상태)
- [x] guard-fail 시나리오 → reject report 영속 + skipped gate rows 포함
- [x] `pnpm typecheck && pnpm test` 그린 유지

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 서버 부트스트랩·기동·조립 e2e (G1 마감)

### 목표
- 서버를 실제로 띄울 수 있게 한다: env 검증 → 스토어 선택 → runner 조립 → listen → graceful shutdown. HTTP 경유 전체 흐름을 e2e로 고정한다.

### 구현 태스크
- [x] `apps/server/src/main.ts` — env 검증(`VIBELOOP_API_TOKEN` 필수, `DATABASE_URL` 있으면 PrismaStore / `VIBELOOP_STORE=memory`면 MemoryStore+경고 로그), `createKernelLoopRunner` 조립, `buildApp(...)` + `listen({port: PORT||3001, host})`
- [x] graceful shutdown — SIGTERM/SIGINT: orchestrator 전부 stop(graceful cancel) → app.close → prisma disconnect → exit 0
- [x] `apps/server/package.json`에 `"start": "node dist/main.js"` + root `package.json`에 `"start:server"` 위임 스크립트
- [x] 루트 `README.md` 신설 — Quickstart(필수 env, docker-compose postgres, migrate, start, 토큰으로 첫 호출 예시) 한 페이지
- [x] 조립 e2e (`apps/server/src/main.e2e.test.ts` 또는 기존 test 확장) — memory store로 실제 listen → HTTP로 project→task→loop 생성(mock agent) → 완료 폴링 → `GET /reports`·`GET /artifacts/*`·SSE 재연결 1회 검증 → 종료

### 자체 테스트
- [x] `pnpm build && pnpm start:server` 기동 → 무토큰 401, 토큰 200 (수동 검증, 출력 기록)
- [x] 조립 e2e 통과 (loop accept + reports 비어 있지 않음)
- [x] SIGTERM 전송 → orchestrator stop 이벤트 후 exit 0, 좀비 프로세스·worktree 잔존 없음
- [x] `VIBELOOP_API_TOKEN` 미설정 기동 → 명확한 에러로 즉시 종료

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. Store 계약 테스트 + seq 원자화 (G2·G5)

### 목표
- MemoryStore로만 검증되던 Store 계약을 PrismaStore에도 동일 적용하고, LoopEvent seq 발급을 원자화한다.

### 구현 태스크
- [x] `apps/server/src/store-contract.test.ts` — Store 인터페이스 계약 스위트: project/task/loop CRUD, idempotency(findLoopByIdempotency·requestHash), active loop 1개, LoopEvent seq 단조, candidate fingerprint unique·dismissed 재제안 차단, orchestrator state/event, Phase 1 신규 메서드(GateRun/AgentRun/WorkspaceRun/Artifact/Report) 전부 포함
- [x] `describe.each([MemoryStore, PrismaStore])` 구조 — PrismaStore는 `TEST_DATABASE_URL` 설정 시에만 실행, 미설정이면 skip + 사유 로그. 각 테스트 전 스키마 정리(truncate)
- [x] docker-compose 기반 로컬 실행 문서화 — README Quickstart에 `docker compose up -d postgres && TEST_DATABASE_URL=... pnpm --filter @vibeloop/server test` 한 줄
- [x] `prisma-store.ts` `addLoopEvent` 원자화 — `$transaction` 내 `max(seq)+1` 산출 + P2002(unique 충돌) 시 1회 재시도. `addOrchestratorEvent`도 동일 패턴이면 함께 적용
- [x] 동시성 테스트 — 같은 loop에 `Promise.all`로 addLoopEvent 10건 → seq 1..10 정확히, 중복·500 없음 (memory/prisma 양쪽)

### 자체 테스트
- [x] 계약 스위트가 MemoryStore에서 전부 통과
- [x] `TEST_DATABASE_URL` 설정 후 PrismaStore에서 동일 스위트 전부 통과 (수동 실행, 출력 기록)
- [x] 동시 10건 seq 테스트 양쪽 통과
- [x] 기존 app.test.ts 17건 그린 유지

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. 스펙 개정 일괄 + baseline 코드 정리 (G3·L1·L2·L3·L4)

### 목표
- 구현 중 정당하게 달라진 부분을 스펙에 역반영하고(드리프트 0), baseline 게이트 선택을 휴리스틱에서 타입 기반으로 정리한다.

### 구현 태스크
- [x] `docs/API_SPEC.md` §6 개정 — "MVP는 in-process runner로 평가를 실행하며 외부 `POST /evaluate` 라우트를 제공하지 않는다" 명문화 (G3) + §5에 loop 생성 body `agent_spec` 옵션 추가 (Phase 1 반영)
- [x] `docs/DB_SCHEMA.md` — Approval.status 값 집합(`pending | approved | rejected | requested_more_tests`) 명문화 (L1) + GateRun·AgentRun·WorkspaceRun·EvalReport·Artifact rows의 영속 주체가 server runner임을 §3에 한 줄
- [x] `docs/SECURITY_MODEL.md` 역반영 (L2) — 전역 protected 기본값에 `SECURITY.md` 포함, 하네스 git 방어에 `GIT_PAGER/GIT_EDITOR/GIT_SEQUENCE_EDITOR/GIT_TERMINAL_PROMPT` 강제 명시
- [x] `docs/EVAL_ENGINE_SPEC.md` — test-integrity 패턴은 substring 매칭(보수적, 오탐 가능·누락 없음) 의미론 한 줄 (L4) + baseline 대상 게이트는 "비교형 타입(security/performance + coverage 산출 게이트)" 기준임을 명시 (L3)
- [x] `packages/eval-engine/src/baseline.ts` — 게이트 선택을 이름 휴리스틱(`includes('test')`)에서 타입 기반으로 변경, 캐시 적중 시 `generated_at` 의미(최근 검증 시각) 주석 (L3)
- [x] 문서 상호참조 정합 점검 — evaluate 라우트 잔존 참조·구 표현 grep 청소

### 자체 테스트
- [x] `pnpm test && pnpm test:e2e` 그린 유지 (baseline 변경 회귀 없음)
- [x] baseline 단위 테스트에 "hard 게이트는 baseline에서 제외" 케이스 추가·통과
- [x] grep 점검: `POST /api/loops/:loopId/evaluate`가 스펙에서 in-process 설명으로만 존재, `requested_more_tests`가 DB_SCHEMA에 정의됨

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 5. CI 워크플로 + smoke 연결 (G4·G6)

### 목표
- "전 스위트 그린"을 로컬 주장에서 CI 보증으로 격상하고, Playwright smoke를 자동 게이트에 연결한다.

### 구현 태스크
- [x] `.github/workflows/ci.yml` — matrix(ubuntu-latest, macos-latest) × Node 22, pnpm cache, 단계: install → typecheck → lint → test → test:e2e
- [x] ubuntu job에 postgres 서비스 컨테이너 추가 → `TEST_DATABASE_URL` 주입으로 Phase 3 PrismaStore 계약 테스트 CI 포함
- [x] root `package.json`에 `"test:smoke"` 추가(apps/web 위임) — ubuntu job에서 playwright 브라우저 설치 후 smoke 실행(서버는 memory store + mock runner 기동, 웹은 dev server)
- [x] `apps/web` build 검증 단계 추가 (`pnpm --filter @vibeloop/web build`)
- [x] README에 CI 상태 배지 + "로컬 전체 검증 명령" 한 줄

### 자체 테스트
- [x] push 후 CI 양 OS 그린 (run URL을 기록)
- [x] PrismaStore 계약 테스트가 CI에서 skip이 아니라 실행됨을 로그로 확인
- [x] smoke가 CI에서 통과, 로컬 `pnpm test:smoke` 1회 통과 (출력 기록)

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 전체 워크스트림 완료 (= EXPERT_REVIEW_3 전 항목 반영, 검토 문서 "반영 상태" 갱신)

## 잔여 리스크 / 후속 과제

- macOS CI에서 e2e의 git/worktree 동작 차이 가능 — 실패 시 해당 fixture만 OS 조건 분기하지 말고 원인 수정 (결정성 우선).
- PrismaStore 계약 테스트의 truncate 전략이 마이그레이션 추가 시 깨질 수 있음 — 테이블 목록을 schema에서 파생.
- 조립 e2e가 실제 포트를 점유 — CI 병렬 실행 대비 포트 0(임의 포트) 사용.
- EXPERT_REVIEW_3의 L5(수동 검증 기록 규칙)는 본 문서 공통 진행 규칙에 반영됨 — 이후 워크스트림에도 동일 적용.
