# implement_20260613_133900.md

작성 일시: `2026-06-13 13:39:00 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

[implement_20260613_130927.md](./implement_20260613_130927.md) Phase 0 영역1 조사 결론(분리 구현)에 따라, **평가자(Evaluator)/Critic 분리**를 advisory only로 구현한다. 비전 §4·9의 "검증자/평가자 분리"를 충족하되, 최종 accept 판정은 계속 deterministic decision engine의 `ALL_PASS`만 담당한다.

핵심 목표는 다음 2개다.

1. 독립 provider로 실행되는 advisory critic/evaluator runner를 도입하고, 품질 점수/지적을 eval-report `advisoryFindings`에만 기록한다.
2. `resolveSameModelReview`를 adapter provider identity 비교로 승격하고, 불명이면 보수적 true를 유지한다.

## 개발 범위

- `packages/agent-adapters`에 advisory critic/evaluator adapter 추가(provider id 노출)
- advisory 게이트 결과에 evaluator 품질 점수/지적을 advisory 블록으로 기록
- `resolveSameModelReview`를 builder provider ↔ reviewer provider 비교로 승격
- evaluator 실패 시 advisory-only 흡수(required 아님 → decision 불참)
- evaluator 출력 redaction(hidden/token/원문 발견 입력 비노출)
- decision 14규칙·accept 경로 불변 회귀 테스트

## 제외 범위

- evaluator 점수를 decision/gate 통과 여부에 반영
- 신규 reason code 추가
- learning/failure ledger (별도: implement_20260613_133901.md)
- convergence 종료 조건 (별도: implement_20260613_133902.md)
- 자동 merge

## 참조 문서

- [implement_20260613_130927.md](./implement_20260613_130927.md) — Phase 0 영역1 발견 기록
- [loop_engineering_notes.md](./loop_engineering_notes.md) — 부록 C 영역1, 비전 §4·9
- [EVAL_ENGINE_SPEC.md](../docs/EVAL_ENGINE_SPEC.md) — §4 advisory 게이트, §8.1 decision 우선순위, same_model_review 의미
- [AUTONOMOUS_LOOP_SPEC.md](../docs/AUTONOMOUS_LOOP_SPEC.md) — advisory provider 독립성 표시

## 문서 기반 제약 사항

- 최종 accept 판정은 deterministic decision engine의 `ALL_PASS`만 허용한다.
- evaluator/critic는 advisory only이며 decision 14규칙에 참여하지 않는다.
- `same_model_review`는 "reviewer provider independence not guaranteed" 표시이며, 불명이면 보수적 true.
- evaluator 출력에 hidden acceptance·token·원문 발견 입력을 노출하지 않는다.

## 예상 영향 범위

| 영역                      | 예상 변경                                         |
| ------------------------- | ------------------------------------------------- |
| `packages/agent-adapters` | advisory critic/evaluator adapter, provider id    |
| `packages/sdk`            | `resolveSameModelReview` 승격, advisory 블록 보강 |
| `docs`                    | EVAL/AUTONOMOUS same_model_review 의미 명문화     |
| `tests`                   | decision 불변 회귀 + provider 비교 단위           |

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- evaluator가 decision에 영향을 주는 코드가 들어가면 즉시 중단한다.
- 각 Phase 종료 시 `pnpm typecheck`, `pnpm lint`, 관련 test를 실행한다.

## Phase 상태 요약

- [ ] Phase 1 완료 — advisory critic/evaluator runner 도입
- [ ] Phase 2 완료 — same_model_review provider 비교 승격
- [ ] Phase 3 완료 — 문서·회귀·전체 검증

---

## Phase 1. advisory critic/evaluator runner 도입

### 목표

독립 provider로 실행되는 evaluator를 advisory 게이트의 한 구현으로 추가한다.

### 구현 태스크

- [ ] `packages/agent-adapters`에 evaluator adapter 추가(provider id 노출, 실행 실패 시 advisory-only)
- [ ] evaluator 품질 점수/지적을 eval-report `advisoryFindings` 블록에 기록
- [ ] evaluator 출력 redaction 적용(hidden/token/원문 발견 입력)
- [ ] evaluator 실패가 required gate 실패로 승격되지 않음 보장

### 자체 테스트

- [ ] evaluator 실패가 accept를 막지 않음(decision 불참) 회귀
- [ ] evaluator 출력에 hidden/token 문자열 미노출
- [ ] advisoryFindings에 점수가 기록되되 decision 14규칙 결과 불변

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 2. same_model_review provider 비교 승격

### 목표

`resolveSameModelReview`를 adapter 종류가 아닌 실제 provider identity 비교로 승격한다.

### 구현 태스크

- [ ] adapter가 provider id를 노출하도록 계약 정리
- [ ] builder provider ↔ reviewer provider 비교로 `resolveSameModelReview` 변경
- [ ] provider 불명/판별 불가 시 보수적 true 유지
- [ ] EVAL_ENGINE_SPEC §8.1 / AUTONOMOUS_LOOP_SPEC에 의미 명문화

### 자체 테스트

- [ ] provider 동일 → true, 상이 → false, 불명 → true(보수적)
- [ ] mock/codex/명령 문자열 변형 케이스 단위 테스트
- [ ] 기존 advisory 회귀 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

---

## Phase 3. 문서·회귀·전체 검증

### 목표

evaluator/same_model_review 변경을 문서화하고 전체 회귀를 확인한다.

### 구현 태스크

- [ ] EVAL_ENGINE_SPEC/AUTONOMOUS_LOOP_SPEC 갱신
- [ ] loop_engineering_notes §13 매트릭스 4·9 항목 갱신

### 자체 테스트

- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm test:e2e`
- [ ] decision 14규칙·accept 경로 불변 최종 회귀
- [ ] hidden/token leak grep 0건
- [ ] `git diff --check`, Prettier check

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 전체 워크스트림 완료

## 잔여 리스크 / 후속 과제

- evaluator provider 독립성은 실제 별도 provider 운영 시에만 완전 보장. 단일 provider 환경에서는 `same_model_review=true` 유지.
- evaluator 비용/지연은 eval.yaml critic 설정으로 on/off 통제.
