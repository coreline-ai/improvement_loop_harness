# implement_20260612_183255.md

작성 일시: `2026-06-12 18:32:55 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

[EXPERT_REVIEW_5_20260612.md](../docs/EXPERT_REVIEW_5_20260612.md)(검토 반영·신뢰 경계 구현 검증)의 발견 사항을 전부 반영하는 **소규모 패치 워크스트림**이다. 높음 0건 상태에서 중간 3건(M1 same_model_review 판정 결함, M2 dev plan 체크박스 불일치, M3 CI actions Node20 종료 임박)과 낮음 5건(L1~L5)을 종결한다.

**시한 주의**: M3은 GitHub Actions의 Node24 강제 전환(2026-06-16)까지 반영되어야 한다.

## 개발 범위

- Phase 1: M1 — same_model_review 판정 로직 교체(보수적 기본값) + 의미 명문화 + 테스트
- Phase 2: M3 — CI actions 버전 인상 + 원격 그린 확인 (시한: 2026-06-16)
- Phase 3: M2 — implement_20260612_061653.md 체크박스 정합화
- Phase 4: L1·L2·L3·L5 일괄 — exec 버퍼 상한, safety-resolve 명시, 문서 상호참조, 검토 배너 갱신

## 제외 범위

- 새 기능 추가 없음 — EXPERT_REVIEW_5에 없는 리팩터링·기능 확장 금지
- LLM critic runner 구현 (M1은 플래그 판정·의미 정정까지만 — runner는 기존 후속 과제)
- 컨테이너 격리, hidden test 보안 저장소 연동, `vibeloop init` (기존 후속 과제 유지)
- 자동 merge (영구 금지)

## 참조 문서

- [docs/EXPERT_REVIEW_5_20260612.md](../docs/EXPERT_REVIEW_5_20260612.md) — 이번 워크스트림의 발견 사항 원본 (M1~M3, L1~L5)
- [이전 계획: implement_20260612_061653.md](./implement_20260612_061653.md) — Phase 3 정합화 대상
- [이전 계획: implement_20260612_061855.md](./implement_20260612_061855.md) — same_model_review·trust summary가 도입된 워크스트림
- [docs/EVAL_ENGINE_SPEC.md](../docs/EVAL_ENGINE_SPEC.md) / [docs/AUTONOMOUS_LOOP_SPEC.md](../docs/AUTONOMOUS_LOOP_SPEC.md) / [docs/TASK_PROTOCOL.md](../docs/TASK_PROTOCOL.md) — Phase 1·4 개정 대상
- [docs/EXPERT_REVIEW_4_20260612.md](../docs/EXPERT_REVIEW_4_20260612.md) — Phase 4에서 배너 갱신 대상

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다 — **상태 요약뿐 아니라 각 Phase 본문 체크박스까지** (5차 검토 M2의 재발 방지).
- 문서에 없는 범위 확장은 하지 않는다.
- **Phase 완료마다 단일 커밋(`fix(review5-phase-N): ...`)을 만든다** — 5차 검토 L4(커밋 granularity 위반)의 재발 방지. 합쳐 커밋 금지.
- 수동 검증(CI run 등)은 기록(URL·출력 로그)을 해당 Phase "이슈 및 수정"에 남긴다.
- `docs/`·`schemas/`가 source of truth — 코드와 충돌 시 스펙 개정 선행.

## Phase 상태 요약

- [x] Phase 1 완료 — same_model_review 판정 교체 (M1)
- [ ] Phase 2 완료 — CI actions 버전 인상 (M3, ~6/16)
- [x] Phase 3 완료 — 061653 체크박스 정합화 (M2)
- [ ] Phase 4 완료 — L 일괄 (L1·L2·L3·L5)

## Phase 1. same_model_review 판정 교체 (M1)

### 목표
- `agentSpec === 'codex'` 문자열 정확 일치 휴리스틱을 제거한다. 플래그의 의미를 "reviewer provider 독립성 미보장"으로 고정하고, **불명이면 보수적으로 true**가 되게 한다 — "다른 모델이 리뷰함"으로 오인되는 역방향 오류(현재 `codex exec ...` 같은 명령 문자열에서 false로 떨어짐)를 차단한다.

### 구현 태스크
- [x] `packages/cli/src/run.ts` L360 판정 교체 — agentSpec 파싱 기반: `mock:` prefix → mock adapter(독립성 논점 없음, false), 그 외 LLM 계열·**판별 불가 → true(보수적)**. `critic.require_different_provider === true`(strict)일 때만 false
- [x] 판정을 순수 함수 `resolveSameModelReview(agentSpec, criticConfig)`로 분리 (`packages/cli/src/` 또는 eval-engine trust 모듈 — 테스트 가능 단위로)
- [x] **스펙 의미 명문화** — `EVAL_ENGINE_SPEC.md` trust summary 정의부(§8.1 인근)에 "same_model_review = reviewer provider independence not guaranteed (모델 동일성 확증이 아님)" 한 단락 + `AUTONOMOUS_LOOP_SPEC.md`에 상호참조 한 줄
- [x] 단위 테스트 — 케이스: `mock:scenario.json` → false / `codex` → true / `codex exec --cd ...`(명령 문자열) → true / 미지 spec → true / strict(require_different_provider) → false
- [x] e2e의 trust_summary 단언이 새 의미와 일치하는지 확인·갱신 (mock agent 기준 false 기대 확인)

### 자체 테스트
- [x] 단위 테스트 5케이스 전부 통과
- [x] `pnpm test && pnpm test:e2e` 그린 유지 (18/18)
- [x] grep으로 `=== 'codex'` 문자열 판정 잔존 0건

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. CI actions 버전 인상 (M3 — 시한 2026-06-16)

### 목표
- Node20 기반 actions의 강제 Node24 전환(6/16) 전에 actions를 최신 메이저로 올리고 원격 그린을 확인한다.

### 구현 태스크
- [ ] `.github/workflows/ci.yml` — `actions/checkout@v4` → 최신 메이저(v5+), `actions/setup-node@v4` → 최신 메이저(v5+), `pnpm/action-setup@v4` → 최신 안정 버전으로 인상
- [ ] 버전 인상에 따른 입력 파라미터 호환 확인 (breaking change 릴리스 노트 점검 — cache 옵션·node-version 표기)
- [ ] main push 후 4개 job(verify ubuntu/macos, prisma-store, web smoke) 그린 + **deprecation annotation 소멸** 확인

### 자체 테스트
- [ ] 원격 CI 전체 그린 (run URL을 "이슈 및 수정"에 기록)
- [ ] run annotation에 Node.js 20 deprecation 경고 0건

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 3. 061653 체크박스 정합화 (M2)

### 목표
- [implement_20260612_061653.md](./implement_20260612_061653.md)의 본문 체크박스 67개(구현 태스크·자체 테스트·이슈·완료 조건)를 실제 상태와 일치시킨다. 문서가 상태 추적 수단이라는 dev-plan 전제를 복원한다.

### 구현 태스크
- [x] EXPERT_REVIEW_5 §4 검증 확인 표를 근거로, 구현·검증이 확인된 항목을 일괄 `[x]` 처리 (Phase 1~5 본문 전체)
- [x] 체크 과정에서 실제로 안 된 항목이 발견되면 체크하지 말고 **그 자리에서 "이슈 및 수정"에 기록** 후 본 워크스트림 Phase 4 이전에 처리 여부 결정
- [x] "이슈 및 수정"의 `- [x] 발견 이슈 없음` 항목들을 실제 이력과 맞게 정리 (exec.ts 재작성 기록이 있는 Phase는 해당 기록 유지)

### 자체 테스트
- [x] `grep -c "^- \[ \]" dev-plan/implement_20260612_061653.md` 결과가 0 (또는 의도적으로 남긴 항목만이며 사유가 기록됨)
- [x] Phase 상태 요약 ↔ 본문 완료 조건 불일치 0건

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. L 일괄 (L1·L2·L3·L5)

### 목표
- 낮음 4건을 한 번에 종결한다 (L4 커밋 규칙은 본 문서 공통 규칙으로 이미 반영).

### 구현 태스크
- [ ] **L1**: `packages/shared/src/exec.ts` — stdout/stderr 누적에 상한(기본 16MB) 도입, 초과 시 이후 청크 폐기 + `…[output truncated at 16MB]` 마커 부가, 로그 파일 기록도 동일 내용으로 일관. 상한은 옵션(`maxBufferBytes`)으로 조정 가능
- [ ] L1 테스트 — 상한 초과 출력 명령 → 결과 문자열이 상한 부근에서 절단 + 마커 포함 + status는 exit code 기준 유지
- [ ] **L2**: exec.ts safety-resolve(5s)의 트레이드오프(SIGKILL 실패 극단 케이스에서 프로세스 잔존 가능 — anti-hang 우선 설계)를 코드 주석 + `EVAL_ENGINE_SPEC.md` gate executor 절에 한 줄 명시
- [ ] **L3**: `TASK_PROTOCOL.md`에 candidate `repro_command`는 표시 전용(실행 금지, AUTONOMOUS_LOOP_SPEC 참조) 한 줄 + `AUTONOMOUS_LOOP_SPEC.md`에 hidden_acceptance 실행 규칙은 EVAL_ENGINE_SPEC가 정의함을 상호참조 한 줄
- [ ] **L5**: `EXPERT_REVIEW_4_20260612.md` 배너의 "최종 CI 결과는 main push 후 별도 확인 대상" 문구를 확인 완료(run 27406109586·27406109586 이후 최신 그린)로 갱신
- [ ] 마감: `EXPERT_REVIEW_5_20260612.md` 반영 상태 배너를 "전 항목 반영 완료"로 갱신 + `docs/README.md` 검토 이력 표기 갱신

### 자체 테스트
- [ ] L1 절단 테스트 통과 + 기존 exec 테스트(타임아웃·kill-tree) 회귀 없음
- [ ] `pnpm typecheck && pnpm lint && pnpm test && pnpm test:e2e` 그린
- [ ] 문서 상호참조 링크 실재 확인 (EVAL↔AUTONOMOUS↔TASK_PROTOCOL)
- [ ] main push 후 원격 CI 그린 (run URL 기록)

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 전체 워크스트림 완료 (= EXPERT_REVIEW_5 전 항목 종결)

## 잔여 리스크 / 후속 과제

- M1의 보수적 기본값(true) 전환으로 기존 report의 same_model_review 값과 의미가 달라짐 — report는 불변 artifact이므로 마이그레이션하지 않고, 스펙에 "값 의미는 schema_version 1.1 이후 생성분 기준" 한 줄로 처리.
- actions 메이저 인상이 self-hosted runner 미사용 전제(GitHub-hosted)에서 검증됨 — runner 환경 변경 시 재확인.
- 기존 후속 과제 유지: 컨테이너 격리, LLM critic runner(이때 same_model_review를 실제 모델 비교로 승격), hidden test 보안 저장소, `vibeloop init`.
