# implement_20260615_064636.md — Frozen Rule Semantic Runner / Adversary Semantic Gate

작성 일시: `2026-06-15 06:46:36 KST`

> 실행 순서: 본 계획은 [RELEASE_GATE_MATRIX.md](../docs/RELEASE_GATE_MATRIX.md)의 **P4 owner**(adversary semantic gate 메커니즘)다. **P2(trust boundary fail-closed)가 선행돼야** 미신뢰 rule 실행이 안전하다. live 검증은 091537 Phase 3.

이 문서는 자가 개선 루프의 **마지막 닫힘 고리**인 _"adversary → M2 → M4 → frozen rulepack → 다음 루프 semantic 고정 게이트"_ 를 코어에 배선하기 위한 개발 계획이다. 즉 **frozen rule semantic runner**(얼린 규칙을 실제로 실행하는 러너)와 **adversary semantic gate**(다음 루프에서 그 규칙을 고정 게이트로 강제)를 구현한다.

> 과장 금지: 상태는 코드 검증 근거(file:line / 테스트)로만 적는다. 미신뢰 LLM 산출 코드를 호스트에서 실행하지 않는다(R1 격리 하드 전제).

## 개발 목적

현재 파이프라인은 adversary 제안을 정적 필터 → M2 격리 confirm → rulepack candidate → replay corpus → replay → **freeze(`frozen_rulepack.json` + `lock_hash`)** 까지 만든다. 그러나 **얼린 규칙이 다음 루프에서 실제 게이트로 실행되지 않는다.** 학습이 "기록"까지만 되고 "강제"되지 않아 루프가 닫히지 않는다.

이 작업의 목적: 얼린 규칙을 **다음 루프의 결정론 hard 게이트로 실행**해, "이번 루프에서 발견·확인·replay된 약점"이 **다음 루프부터 영구적으로 막히게** 한다. (§14 "학습이 안전하게 들어가는 자리"의 실행 부분.)

## 현재 상태 분석 (코드 근거)

### 이미 구현됨 (외부 추가 포함, wired)

| 단계                       | 위치                                                                                      | 비고                                                                                                    |
| -------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 정적 필터                  | `packages/eval-engine/src/adversary-filter.ts` `filterAdversaryProposal()`                | scope/objective/no-weakening/no-hidden-leak/bounded-cost (정적, 코드 미실행)                            |
| M2 격리 confirm            | `packages/eval-engine/src/adversary-execution.ts` `confirmProposalUnderIsolation()`       | R1 컨테이너에서 base=fail ∧ candidate=pass 결정론 확인                                                  |
| adversary review(advisory) | `packages/sdk/src/adversary-review.ts` + `improvement-loop.ts`(배선)                      | `authority: advisory_only`, `decision_impact: none` — accept 불참                                       |
| candidate/replay/freeze    | `packages/sdk/src/rulepack-{candidate,replay-corpus,replay,freeze}.ts`                    | CLI: `adversary-confirm` → `adversary-rulepack-candidate` → `…-replay-corpus` → `…-replay` → `…-freeze` |
| shadow 승격 안전핵         | `packages/eval-engine/src/rulepack-shadow.ts` `diffRulepack`/`decideShadowPromotion`      | append-only ∧ replaySafe ∧ not-applied-this-loop ∧ has-new-rules                                        |
| replay 실행 substrate      | `packages/eval-engine/src/rulepack-replay.ts` `replayCorpusUnderIsolation()`              | R1에서 ReplayCase(`command`,`expect`) 실행                                                              |
| frozen 무결성 게이트       | `packages/eval-engine/src/builtin.ts` `checkRulepackLock()` (`builtin:rulepack-lock`)     | **형식/해시/append-only/replaySafe만 검증**                                                             |
| eval 설정 슬롯             | `schemas/eval.schema.json` `rulepack_lock`, `packages/task-protocol` `RulepackLockConfig` | `{file, required_authority, required_decision_impact}`                                                  |

### 진짜 공백 (이번에 구현)

1. **실행 가능 규칙 모델 부재** — `RulepackRule = {id, hash}` (`rulepack-shadow.ts:19`). 규칙을 실행할 **semantic spec(테스트 본문/명령 + 기대 verdict)** 이 없다. `FrozenRulepack.rules`도 해시 참조뿐.
2. **semantic runner 부재** — 얼린 규칙을 candidate worktree에 대해 **실제 실행**해 pass/fail을 내는 코드가 없다. (replay는 *제안*을 검증하는 별도 harness이지, 다음 루프 게이트가 아님.)
3. **게이트 주입 부재** — `frozen_rulepack.json`을 로드해 규칙을 `EvalGate`처럼 `runGates`에 넣는 경로가 없다. `builtin:rulepack-lock`은 **검증만** 한다(`builtin.ts:130`).
4. **다음-루프 적용 경로 부재** — 루프 N의 frozen 팩이 루프 N+1의 eval 계약으로 흘러 게이트가 되는 배선이 없다.
5. **실행 시점 provenance 부재** — 실행되는 규칙 본문이 `lock_hash`에 묶인 내용과 동일함을 보장하는 검증이 없다(semantic drift 위험).

작업 착수 당시 §17 로드맵도 이를 명시했다: _"M4 replay-corpus EXECUTION(룰 실행)은 rulepack-runner/policy.lock 인프라 의존. 미구현."_ 현재는 core runner/gate/inspect까지 구현됐고, live adversary/broad corpus 증거가 남아 있다.

## 개발 범위

- `RulepackRule`에 **content-addressed 실행 spec** 추가(테스트 본문 + 기대 verdict). `lock_hash`가 실행 내용을 덮도록.
- **semantic runner**: frozen 규칙을 candidate worktree에 대해 **R1 격리 실행** → 규칙별 결정론 pass/fail → 집계.
- **builtin 게이트**(`builtin:rulepack-semantic`)와 eval 설정 블록 + 스키마 + dispatch + `runGates` 통합 + decision reason.
- **다음-루프 적용**: 루프 N frozen 팩 → 루프 N+1 게이트. `improve`/`orchestrate`/CLI에 적용 경로.
- 실행 시점 **provenance 바인딩**(실행 내용 ↔ `lock_hash`) + 안전 불변식.

## 제외 범위 (Non-goals, §18 정렬)

- 검증자/평가자를 LLM으로 만들지 않는다(러너는 결정론).
- **현재 루프**의 통과 바를 자동으로 낮추지 않는다(append-only 강화만, 적용은 next-loop-only).
- hidden acceptance/adversary 원문을 Builder/Skill에 노출하지 않는다.
- 미신뢰 코드를 호스트에서 실행하지 않는다(R1 필수).
- policy/rulepack을 hot loop 중 변경하지 않는다.

## 참조 문서

- [SELF_IMPROVEMENT_LOOP_DESIGN.md](../docs/SELF_IMPROVEMENT_LOOP_DESIGN.md) §9·§13·§14·§17 (rulepack/policy.lock/학습/로드맵)
- [implement_20260613_160413.md](./implement_20260613_160413.md) (rulepack-runner/policy.lock Phase 2 의존 언급)
- [implement_20260614_193356.md](./implement_20260614_193356.md) (trust-floor; provenance·격리 패턴 재사용)
- [SKILL_REAL_USER_SCENARIO.md](../docs/SKILL_REAL_USER_SCENARIO.md) §8 (B2/B3 provenance·재검증 패턴 재사용)

## 공통 진행 규칙

- 순서는 아래 `우선 구현 순서`를 따른다. 앞 Phase 자체 테스트 통과 전 뒤 Phase 완료 체크 금지.
- 모든 규칙 실행은 **R1 격리**에서만(미구현/런타임 부재 시 fail-closed: 게이트 error → 보수적 처리).
- 얼린 규칙은 **append-only·replaySafe·next-loop-only** 인 것만 실행한다(실행 직전에도 재확인).
- 실행 내용은 `lock_hash`에 묶인 것과 **해시 일치**해야 한다(불일치 → reject).
- `PASS`/`프로덕션급` 표현은 증거 범위와 함께만.

## Phase 상태 요약

- [x] Phase 1 — 실행 가능 규칙 모델(content-addressed RuleSpec) + freeze 확장
- [x] Phase 2 — Frozen Rule Semantic Runner (eval-engine, R1 실행) — core runner + mock known-good/bad + docker-gated test 추가
- [x] Phase 3 — `builtin:rulepack-semantic` 게이트 + eval 설정/스키마 + decision 통합
- [x] Phase 4 — 다음-루프 적용 배선 (loop N → N+1) + local artifact end-to-end
- [x] Phase 5 — 안전 불변식·provenance·CLI·문서 — inspect CLI/문서/eval-report provenance 완료

## 전문가 리뷰 결론 — 우선 구현 순서

> ⚠️ 아래 `P1~P5`은 **본 계획 내부 Phase 순서**다 — [RELEASE_GATE_MATRIX](../docs/RELEASE_GATE_MATRIX.md)의 게이트 `P0~P5`와는 다른 축이다. 본 계획 전체가 매트릭스 게이트 **P4**(adversary semantic gate)에 해당하며, **게이트 P2(trust boundary)가 선행**돼야 미신뢰 rule 실행이 안전하다.

```text
P1 실행 가능 규칙 모델(spec + hash 바인딩)   ← 데이터 계약 먼저
→ P2 semantic runner(R1 실행, 결정론 verdict)
→ P3 builtin 게이트 + eval 설정 + decision reason
→ P4 다음-루프 적용 + end-to-end(학습이 실제로 강제됨)
→ P5 안전 불변식·provenance·CLI·문서
```

| 우선 | Phase | 먼저 하는 이유                                                               | 완료 전 금지          |
| ---- | ----- | ---------------------------------------------------------------------------- | --------------------- |
| P1   | 1     | spec/hash 계약이 없으면 러너·게이트가 실행할 대상이 없다. drift 방지의 토대. | 러너 구현             |
| P2   | 2     | 격리 실행 결정론 verdict가 게이트의 심장. replay substrate 재사용.           | 게이트 배선           |
| P3   | 3     | 러너를 `runGates`/decision에 연결해야 "게이트"가 된다.                       | 다음-루프 적용        |
| P4   | 4     | 루프 N→N+1 흐름이 닫혀야 "학습 강제"가 실증된다.                             | 안전 선언             |
| P5   | 5     | 불변식/provenance/문서로 신뢰·재현성 확정.                                   | "닫힘 루프 완료" 선언 |

### 리뷰 발견 사항

| 등급 | 항목                             | 조치                                                                                |
| ---- | -------------------------------- | ----------------------------------------------------------------------------------- |
| High | 미신뢰 코드 실행                 | R1 격리 하드 전제. 런타임 부재 시 게이트 error(보수).                               |
| High | semantic drift(실행 내용 ≠ 해시) | RuleSpec를 content-addressed로, 실행 직전 해시 재검증.                              |
| High | 현재 루프 바 낮추기 위험         | next-loop-only 강제: frozen 팩의 producing loop id ≠ 현재 loop. append-only 재확인. |
| Med  | 규칙 비결정성                    | 규칙은 결정론 exit-code만(시간/네트워크 의존 금지, network=none).                   |
| Med  | hidden 누설                      | 규칙 본문/출력에 hidden marker 스캔(artifact-leak 재사용).                          |

## Phase 1. 실행 가능 규칙 모델 (content-addressed RuleSpec)

### 목표

규칙을 **실행 가능**하게 만들고, 그 실행 내용을 `lock_hash`가 덮게 한다(드리프트 차단).

### 구현 태스크

- [x] `RulepackRule`(`rulepack-shadow.ts`)을 확장하거나 `RuleSpec`를 신설: `{ id, hash, spec: { kind: 'command_test', body?, target_path?, command, expect: 'fail_to_pass'|'pass_to_pass', network: 'none' } }`. `hash = sha256(canonical(spec))` 로 **내용 주소화**.
- [x] freeze 경로(`sdk/src/rulepack-freeze.ts`, `rulepack-candidate.ts`)가 M2-confirm된 proposal의 **실행 spec**(본문+기대 verdict)을 규칙에 담도록. `FrozenRulepack.rules`가 spec 포함 규칙을 보존한다(legacy `{id,hash}`도 호환).
- [x] `lock_hash` 계산에 spec 포함(이미 rules가 들어가면 자동). 기존 `checkRulepackLock`이 새 형식에서도 통과하도록 호환 유지하고, spec hash mismatch는 `RULEPACK_LOCK_RULE_SPEC_HASH`로 fail-closed.
- [x] adversary proposal → RuleSpec 변환기(`ruleSpecForProposal`)를 candidate 빌더에 추가.

### 자체 테스트

- [x] proposal → RuleSpec → 해시 안정성(동일 spec 동일 hash) 단위 테스트.
- [x] freeze 산출 `frozen_rulepack.json`에 실행 spec이 포함되고 `lock_hash`가 spec 변조 시 바뀜.
- [x] 기존 `checkRulepackLock`(무결성) 회귀 통과.

### 완료 조건

- [x] frozen 팩이 **실행 가능 규칙**을 content-addressed로 담는다 + 무결성 게이트 회귀 통과.

## Phase 2. Frozen Rule Semantic Runner (R1 실행)

### 목표

얼린 규칙을 candidate worktree에 대해 **격리 실행**해 규칙별 결정론 verdict를 낸다.

### 구현 태스크

- [x] `packages/eval-engine/src/rulepack-runner.ts` 신설: `runFrozenRulepack(frozen, { worktreePath, image, network: 'none', timeoutMs }) → { allPass, results: [{ ruleId, status, expected, actual }], errors }`.
- [x] 각 규칙: spec.body가 있으면 worktree에 **임시 materialize** → `runCommandInContainer`(`@vibeloop/shared`, replay와 동일 substrate)로 실행 → exit-code로 candidate pass 여부 판정 → 실행 후 정리(finally rm/restore).
- [x] **실행 직전 provenance**: 실행할 spec의 `sha256(canonical(spec))`가 frozen 팩 기록 hash와 일치하는지 확인. 불일치 → 규칙 error(보수).
- [x] 런타임 부재(`isContainerRuntimeAvailable()` false) → 전체 error(fail-closed), 호스트 실행 금지.
- [x] hidden marker/누설 스캔(규칙 본문·출력)에 artifact-leak 유틸 재사용.

### 자체 테스트 (docker-gated, replay 테스트 패턴)

- [x] 알려진-나쁜 candidate가 규칙에 걸려 `fail`(mock runner 단위 테스트, allPass=false) — known-bad pass 없음.
- [x] 알려진-좋은 candidate는 규칙 통과(mock runner 단위 테스트, allPass=true).
- [x] spec 변조(hash 불일치) → 규칙 error(실행 안 함).
- [x] 런타임 부재 → error(fail-closed), 호스트 미실행.
- [x] 규칙 본문·출력 artifact leak → error(fail-closed), raw literal 미보고.
- [x] docker-gated real container runner test 추가(런타임 없는 현재 환경에서는 skip).

### 완료 조건

- [x] frozen 규칙이 격리에서 결정론으로 실행되고 known-bad를 잡는다(함수 단위 검증).

## Phase 3. `builtin:rulepack-semantic` 게이트 + 설정/스키마 + decision

### 목표

러너를 실제 **게이트**로 만들어 `runGates`/decision에 연결한다.

### 구현 태스크

- [x] `EvalConfig`에 설정 추가: `rulepack_semantic?: { file, image, network: 'none', timeout_ms, current_loop_id }`. (`task-protocol/src/types.ts`, `schemas/eval.schema.json`.)
- [x] `packages/eval-engine/src/builtin.ts` dispatch에 `case 'rulepack-semantic'` 추가 → `checkRulepackSemanticGate(context)`: frozen 팩 lock 검증 → `runFrozenRulepack(...)` → violations 집계 → `GuardCheckResult`.
- [x] `GateRunContext`(worktreeRoot/evalConfig/artifactRoot)로 동작. 기본 `type: 'integrity'`.
- [x] decision reason: 현재는 required builtin fail의 기존 `GATE_REQUIRED_FAILED` 경로 재사용.
- [x] `runGates` 통합은 builtin 경로 자동(`orchestrator.ts` `isBuiltinGate`).

### 자체 테스트

- [x] eval에 `builtin:rulepack-semantic`(required) + frozen 팩 지정 → known-bad candidate가 `fail`/reject 경로로 수렴(게이트 단위).
- [x] known-good candidate는 semantic gate pass.
- [x] frozen 팩 무결성 실패(append-only 위반/lock_hash 불일치) → 게이트 fail/reject(러너 진입 전 차단).

### 완료 조건

- [x] 얼린 규칙이 **required 게이트**로 작동해 위반 candidate를 reject 경로로 보낸다(게이트 단위).

## Phase 4. 다음-루프 적용 배선 (loop N → N+1) + end-to-end

### 목표

루프 N에서 만든 frozen 팩이 **루프 N+1의 게이트**로 자동 강제됨을 실증한다.

### 구현 태스크

- [x] 적용 경로(1차): `orchestrate --generate-eval --eval-rulepack-semantic <frozen.json> --eval-rulepack-semantic-image <image>`로 frozen 팩을 다음 루프 eval 계약에 주입. **next-loop-only 강제**: frozen `source_loop_id`가 현재 loop와 같으면 게이트 error.
- [x] 적용 경로(2차): `improve --rulepack-semantic <frozen.json> --rulepack-semantic-image <image>`가 기존 `--eval` 위에 required `builtin:rulepack-semantic` overlay eval을 생성해 실행. `--loop-id`가 없으면 overlay와 실제 실행이 같은 generated loop id를 공유한다.
- [x] (선택) `orchestrate --carry-rulepack <frozen.json> --carry-rulepack-image <image>`가 기존 `--eval` 또는 `--generate-eval` 계약에 frozen semantic gate를 승계한다. 기존 eval은 `eval.carry-rulepack.json` overlay로 보존하며, 출력 JSON에 `carried_rulepack` evidence를 남긴다.
- [ ] end-to-end: 루프 N에서 adversary→M2→M4→freeze로 규칙 생성 → 루프 N+1에서 그 규칙을 위반하는 candidate를 주입 → semantic 게이트가 reject.

### 자체 테스트

- [x] e2e: 동일 repo, 루프1 freeze → 루프2에서 위반 candidate reject / 정상 candidate accept(SDK artifact 통합 테스트, mock R1 runner).
- [x] next-loop-only: 같은 loop id에 적용 시 게이트 error(자가 적용 금지).

### 완료 조건

- [x] "확인→replay→freeze→다음 루프 강제"가 한 흐름으로 통과(SDK artifact 통합). live adversary 발견 lane은 091537 Phase 3에서 별도 검증.

## Phase 5. 안전 불변식·provenance·CLI·문서

### 구현 태스크

- [x] 불변식 게이트화(1차): 실행 직전 ① append-only ② replaySafe ③ next-loop-only ④ command-test/network none ⑤ R1 runtime ⑥ artifact leak 스캔 재확인(하나라도 실패 → 보수 처리).
- [x] provenance: eval-report에 `rulepack_semantic`(적용 팩 hash, 규칙별 verdict, 실행 격리 메타) 기록. selection/eval-report 해시 결속(B3 패턴).
- [x] CLI: `vibeloop rulepack inspect <frozen.json>`(요약·검증).
- [x] 적용 플래그 문서화: `orchestrate --generate-eval`의 `--eval-rulepack-semantic`, `--eval-rulepack-semantic-image`, `--eval-rulepack-semantic-timeout-ms`, `orchestrate`의 `--carry-rulepack`, `--carry-rulepack-image`, `--carry-rulepack-timeout-ms`, `improve`의 `--rulepack-semantic`, `--rulepack-semantic-image`, `--rulepack-semantic-timeout-ms`.
- [x] 문서(1차): `EVAL_ENGINE_SPEC.md`, `SKILL_REAL_USER_SCENARIO.md`, Skill usage, `SELF_IMPROVEMENT_LOOP_DESIGN.md` §17, README에 semantic gate core 반영.

### 자체 테스트

- [x] 주요 불변식 위반 주입 시 fail-closed(hash mismatch, runtime unavailable, same-loop, artifact leak, unsafe lock).
- [x] provenance 변조 → reject.
- [x] `pnpm typecheck/lint/test/test:e2e` 전부 green(2026-06-15 재확인).

### 완료 조건

- [x] 안전 불변식·provenance가 테스트로 보장되고 문서가 코드와 일치.

### Phase 5 진행 증거

- 코드 근거: `packages/eval-engine/src/builtin.ts`, `packages/eval-engine/src/report/eval-report.ts`, `schemas/eval-report.schema.json`, `packages/sdk/src/run.ts`.
- 추가 보강: `builtin:rulepack-semantic` stdout log에 `details.rulepack_semantic`(lock hash, source/current loop id, image/network, 규칙별 verdict, errors)을 기록하고, eval-report는 `rulepack_semantic[]`으로 이를 요약한다. 해당 gate stdout/stderr는 기존 `provenance.gate_artifact_hashes`에 포함되어 변조 시 provenance 검증이 실패한다.
- 추가 보강: `packages/task-protocol/src/eval-config.ts`의 builtin allowlist에 `rulepack-semantic`을 추가하고, `packages/cli/src/commands/improve.ts`가 기존 eval을 수정하지 않는 overlay eval(`projects/<project>/improve/<loop>/eval.rulepack-semantic.json`)로 frozen semantic gate를 직접 적용한다.
- 추가 보강: `packages/cli/src/commands/orchestrate.ts`가 `--carry-rulepack`으로 기존 eval 또는 generated eval에 frozen semantic gate를 승계한다. 기존 eval은 `projects/<project>/orchestrate/<loop>/eval.carry-rulepack.json` overlay로 남고, invalid lock은 PR candidate 0으로 fail-closed 된다.
- 검증: `corepack pnpm exec vitest run packages/eval-engine/src/eval-engine.test.ts packages/eval-engine/src/decision-report.test.ts packages/sdk/src/sdk.test.ts packages/cli/src/index.test.ts` → 4 files, 123 tests passed.
- 추가 검증: `corepack pnpm typecheck` 통과.

## 안전 불변식 (요약)

1. 미신뢰 규칙은 **R1 격리(network=none)** 에서만 실행. 런타임 부재 → fail-closed.
2. 실행 내용 = `lock_hash`에 묶인 spec(해시 일치). 불일치 → 미실행·reject.
3. **next-loop-only**: producing loop ≠ 현재 loop. 자가 적용 금지.
4. **append-only·replaySafe** 인 frozen 팩만 실행(실행 직전 재확인).
5. 규칙은 **결정론**(exit-code)만. 비결정/네트워크/시간 의존 금지.
6. hidden/secret 비노출(본문·출력 스캔).
7. 현재 루프 통과 바를 낮추지 않음(강화 방향, 다음 루프부터).

## 완료 조건 (전체)

- [x] Phase 1–4 구현 + 자체 테스트 통과(docker-gated 포함, 현재 런타임 부재 시 skip).
- [x] e2e: 루프 N freeze → 루프 N+1 semantic 게이트가 위반 candidate를 reject(local artifact integration).
- [x] 안전 불변식·provenance 테스트 통과, 문서 일치.
- [x] §17 로드맵의 M2/M4 EXECUTION이 "구현"으로 갱신될 근거 확보. 단 live adversary lane과 broad semantic corpus evidence는 091537 Phase 3/P5 잔여다.
