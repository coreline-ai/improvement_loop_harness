# implement_20260610_223129.md

작성 일시: `2026-06-10 22:31:29 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

VibeLoop Harness의 검증 커널을 CLI로 먼저 완성하고(MVP-0), 그 위에 single agent loop(MVP-1), 웹 report viewer(MVP-2), GitHub Draft PR(MVP-3)을 순서대로 올린다. 커널의 완성 기준은 "하나의 candidate patch를 `accept | reject | needs_human_review | needs_more_tests`로 deterministic하게 판정하고 eval-report.json을 항상 남기는 것"이다.

## 개발 범위

- Phase 1~11: MVP-0 CLI 검증 커널 (모노리포 → task-protocol → artifacts → workspace-runner → guards → eval 코어 → 증거 검증 → decision/report → agent adapter/proxy → CLI 배선 → fixture e2e)
- Phase 12: MVP-1 마감 (정적 HTML report, retention gc)
- Phase 13~14: MVP-2 (Prisma DB + Fastify API, Next.js report viewer)
- Phase 15: MVP-3 (GitHub Draft PR)
- Phase 16~17: MVP-4 자율 개선 루프 — 봇이 문제를 발견하고, 1개씩 순차로 수정 루프를 돌리고, 고정 결과지 통과분만 PR로 만들고, 반복 ([AUTONOMOUS_LOOP_SPEC.md](../docs/AUTONOMOUS_LOOP_SPEC.md))

## 제외 범위

- 멀티 tenant billing, 자동 merge(전 모드 영구 금지), agent rotation, 스킬 마켓, 대규모 병렬 실행
- Docker/컨테이너 격리, OS 수준 네트워크 차단 (MVP 이후)
- 멀티 유저 RBAC (MVP는 단일 토큰)
- LLM critic runner 자체 구현 (advisory 명령 게이트 실행 경로까지만 — 후속 과제)
- `vibeloop init` 대상 repo 템플릿 생성기 (후속 과제)
- 멀티 프로젝트 병렬 orchestration, CI 로그·이슈 트래커·에러 모니터링 수집기, LLM 기반 candidate 고도화 (AUTONOMOUS_LOOP_SPEC §8 — 후속)
- learnings/SKILL.md 자동 반영 (MVP-5 Skill Manager)
- Windows 지원

## 참조 문서

- [docs/README.md](../docs/README.md) — 명세 인덱스
- [docs/PRODUCT_SPEC.md](../docs/PRODUCT_SPEC.md) / [docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) — 제품·커널 16단계 흐름
- [docs/LOOP_STATE_MACHINE.md](../docs/LOOP_STATE_MACHINE.md) — 상태·retry 4모드·idempotency
- [docs/TASK_PROTOCOL.md](../docs/TASK_PROTOCOL.md) / [docs/EVAL_ENGINE_SPEC.md](../docs/EVAL_ENGINE_SPEC.md) — 계약·실행 순서·decision 12규칙
- [docs/SECURITY_MODEL.md](../docs/SECURITY_MODEL.md) — 신뢰 경계·git 방어·env/proxy 정책
- [docs/ARTIFACT_SCHEMA.md](../docs/ARTIFACT_SCHEMA.md) / [docs/API_SPEC.md](../docs/API_SPEC.md) / [docs/DB_SCHEMA.md](../docs/DB_SCHEMA.md)
- [docs/MVP_IMPLEMENTATION_PLAN.md](../docs/MVP_IMPLEMENTATION_PLAN.md) — fixture 16종·금지사항
- [docs/AUTONOMOUS_LOOP_SPEC.md](../docs/AUTONOMOUS_LOOP_SPEC.md) — MVP-4 바깥 루프: 발견·candidate 큐·orchestrator·guardrails
- [schemas/task.schema.json](../schemas/task.schema.json), [schemas/eval.schema.json](../schemas/eval.schema.json), [schemas/eval-report.schema.json](../schemas/eval-report.schema.json)
- [docs/EXPERT_REVIEW_1_20260610.md](../docs/EXPERT_REVIEW_1_20260610.md), [docs/EXPERT_REVIEW_2_20260610.md](../docs/EXPERT_REVIEW_2_20260610.md) — 검토 이력

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- Phase 완료마다 단일 커밋(`feat(phase-N): ...`)을 만든다.

## 문서 기반 제약 사항

- 웹 UI/DB CRUD부터 구현 금지 — Phase 13~14는 Phase 11(e2e) 통과 후에만.
- 가드 통과 전 프로젝트 명령 게이트 실행 금지 (fail-fast, EVAL_ENGINE_SPEC §3).
- 가드는 하네스 내장(builtin) — 대상 repo 스크립트 위임 금지.
- secret을 agent env에 전달 금지 — LLM은 localhost proxy 경유.
- worktree/artifact를 대상 repo 내부에 배치 금지 — `~/.vibeloop` 데이터 디렉터리.
- agent에게 eval-report.json 직접 작성시키기 금지.
- `docs/`·`schemas/`가 source of truth — 코드와 충돌 시 스펙 개정 절차 선행.
- 기술 스택 고정: TypeScript + Node 22, pnpm workspaces, vitest, execa, ajv(2020-12), Fastify, Prisma+PostgreSQL(Phase 13부터), git ≥ 2.31.

## Phase 상태 요약

- [ ] Phase 1 완료 — 모노리포 기반
- [ ] Phase 2 완료 — task-protocol
- [ ] Phase 3 완료 — artifacts
- [ ] Phase 4 완료 — workspace-runner
- [ ] Phase 5 완료 — guards
- [ ] Phase 6 완료 — eval gate 실행 코어
- [ ] Phase 7 완료 — baseline·test-on-base·evidence
- [ ] Phase 8 완료 — decision engine·eval-report
- [ ] Phase 9 완료 — agent adapter·LLM proxy
- [ ] Phase 10 완료 — CLI 커널 배선
- [ ] Phase 11 완료 — fixture e2e (MVP-0 완료 게이트)
- [ ] Phase 12 완료 — HTML report·gc (MVP-1)
- [ ] Phase 13 완료 — DB·API 서버
- [ ] Phase 14 완료 — 웹 report viewer (MVP-2)
- [ ] Phase 15 완료 — GitHub Draft PR (MVP-3)
- [ ] Phase 16 완료 — Improvement Discovery·candidate 큐 (MVP-4)
- [ ] Phase 17 완료 — Loop Orchestrator·guardrails (MVP-4)

## Phase 1. 모노리포 기반

### 목표
- git repo 초기화와 pnpm/TS/vitest/lint 기반, 공용 exec 유틸 확립.

### 구현 태스크
- [ ] `git init` + `.gitignore`(node_modules/dist/.vibeloop/coverage) 작성
- [ ] `pnpm-workspace.yaml`(packages/*, apps/*) + 루트 `package.json` scripts(build/test/test:e2e/typecheck/lint)
- [ ] `tsconfig.base.json`(strict, NodeNext) + 패키지 상속 패턴
- [ ] `vitest.workspace.ts` + eslint(typescript-eslint) + prettier
- [ ] `packages/shared/src/exec.ts` — `runCommand(cmd, {cwd, env, timeoutMs, stdoutFile, stderrFile})` (execa, timeout 시 kill-tree → status 'error')
- [ ] `packages/shared/src/types.ts`(GateResult·ChangedFile·Decision 공용 타입) + `src/data-dir.ts`(`VIBELOOP_DATA_DIR` → `~/.vibeloop`)
- [ ] `packages/cli` 뼈대 — `bin/vibeloop` → commander `--version`

### 자체 테스트
- [ ] `pnpm typecheck && pnpm lint && pnpm test` exit 0
- [ ] `runCommand('echo hi')` → exit 0, stdout 파일 기록 확인
- [ ] `runCommand('sleep 10', {timeoutMs:200})` → 'error' + 잔존 프로세스 없음
- [ ] `vibeloop --version` 출력 확인

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 2. task-protocol — 스키마 로더·검증

### 목표
- schemas/ 3종을 코드 계약으로: 로드·검증·limits 병합·변수 검증·risk 분류.

### 구현 태스크
- [ ] `packages/task-protocol/src/schema.ts` — Ajv2020에 3종 스키마 등록, `validateOrThrow` (instancePath 포함 에러)
- [ ] `src/task.ts` — `loadTask(path)`: yaml→스키마 검증→의미 검증(경로 정규화, `..`·절대경로 거부)
- [ ] `src/eval-config.ts` — `loadEvalConfig(path)`: 스키마 검증 + 게이트 순서 규범(scope/integrity가 프로젝트 명령 게이트보다 선행, 위반 시 EvalConfigError) + `builtin:` 이름 화이트리스트
- [ ] `src/limits.ts` — `mergeLimits`: task/eval 중 엄격한 값
- [ ] `src/interpolation.ts` — 허용 변수 5종(`TASK_FILE`,`BASE_COMMIT`,`LOOP_ID`,`WORKTREE_ROOT`,`ARTIFACT_ROOT`) 외 `${VAR}` 거부
- [ ] `src/risk.ts` — `classifyRisk(paths, risk_classification)` → `{areas[], unknown}`
- [ ] valid/invalid yaml fixture 각 3종 이상 (순서 위반 eval.yaml 포함)

### 자체 테스트
- [ ] TASK_PROTOCOL §2·EVAL_ENGINE_SPEC §10 예시 yaml 그대로 검증 통과
- [ ] hard 게이트가 scope 게이트보다 앞인 eval.yaml → EvalConfigError
- [ ] `mergeLimits({lines:300},{lines:500})` → 300
- [ ] `${UNKNOWN}` 포함 command → 거부 / auth 경로 → areas=['auth']

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 3. artifacts — 증거 보관소

### 목표
- run dir 레이아웃·redaction·manifest/checksum·retention. 불변성 보장.

### 구현 태스크
- [ ] `packages/artifacts/src/layout.ts` — `createRunDir`: `input/ workspace/ patches/ logs/gates/ reports/ metrics/ integrity/` 생성 + 경로 헬퍼
- [ ] `src/writer.ts` — `writeArtifact`: runDir 밖 escape 거부(realpath), redactor 훅 경유
- [ ] `src/redaction.ts` — 주입된 secret 값 + key/token/password 패턴 마스킹
- [ ] `src/manifest.ts` — init/finalize, finalize 시 전 파일 sha256 필수 기록, 이후 쓰기 throw
- [ ] `src/retention.ts` — decision별 보존일(180/30/7) `expires_at` 기록 + `collectExpired`

### 자체 테스트
- [ ] createRunDir 후 하위 디렉터리 8종 존재
- [ ] finalize 후 checksum 재계산 대조 일치 + 추가 쓰기 throw
- [ ] `../../x` 쓰기 거부 / secret 문자열 로그에서 마스킹
- [ ] rejected → expires_at = +30일

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 4. workspace-runner — 격리 작업공간

### 목표
- 방어 플래그 git wrapper, repo 밖 worktree, deps 캐시, env scrub, .git 스냅샷.

### 구현 태스크
- [ ] `packages/workspace-runner/src/git.ts` — `safeGit`: 모든 git 호출 단일 진입점, `-c core.hooksPath=/dev/null -c core.fsmonitor=` + `GIT_CONFIG_GLOBAL/SYSTEM=/dev/null GIT_TERMINAL_PROMPT=0`
- [ ] `src/base-commit.ts` — branch→SHA 고정, 미존재 branch 에러
- [ ] `src/worktree.ts` — `<dataDir>/projects/<id>/worktrees/<loop-id>` 생성/제거/prune + repo별 lockfile로 git 변이 직렬화
- [ ] `src/deps.ts` — lockfile sha256 키 캐시 복사, 미스 시 `npm ci`/`pnpm install --frozen-lockfile` 후 적재(에이전트 실행 전만), 메인 repo node_modules 공유 금지
- [ ] `src/env.ts` — allowlist(`PATH`,`CI`,`NODE_ENV`,`VIBELOOP_*`) + ephemeral HOME, 차단 키 단언
- [ ] `src/snapshot.ts` — `.git/config`·`.git/hooks/**`·`.git/worktrees/<id>/**` 해시 스냅샷
- [ ] `tests/helpers/repo.ts` — 임시 git repo 생성 헬퍼 (이후 전 Phase 재사용)

### 자체 테스트
- [ ] worktree HEAD = baseCommit, 위치가 대상 repo 밖
- [ ] scrubEnv 결과에 `ANTHROPIC_API_KEY`·`GITHUB_TOKEN` 부재 (원 env에 있어도)
- [ ] 악성 `core.pager` 설정 후 safeGit('status') → 부작용 없음
- [ ] deps 2회째 캐시 적중 / 동시 worktree 생성 2건 lock 직렬화

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 5. guards — diff 추출 + builtin 가드 5종

### 목표
- 전 가드를 "(입력) → 판정" 순수 함수로. diff는 base_commit 대비(HEAD 금지).

### 구현 태스크
- [ ] `packages/guards/src/diff.ts` — `git diff <base> --name-status -z` + `status --porcelain=v2 -z`(untracked/rename/mode 120000) → ChangedFile[] + `candidate.patch` + `changed-files.json`, patch 적용 헬퍼 `applyPatch(target, patch, {includeOnly?})` 포함(test-on-base·retry_eval_only·PR 재사용)
- [ ] `src/git-meta-integrity.ts` — Phase 4 스냅샷 전후 비교
- [ ] `src/protected-files.ts` — prefix·glob(`.env.*`) 매칭
- [ ] `src/diff-scope.ts` — `protected > forbidden > allowed` 우선순위, untracked 포함, symlink 무조건 위반
- [ ] `src/limits.ts` — max_changed_files/lines 검사
- [ ] `src/test-integrity.ts` — 변경 테스트 파일 한정 forbidden/suspicious 패턴 (eval.yaml `test_integrity` 주입)

### 자체 테스트
- [ ] 수정+untracked+rename 혼합 → 3건 전부 검출
- [ ] agent가 자체 commit 후에도 diff 누락 없음
- [ ] `.git/hooks` 추가 → git-meta fail / scope 밖 symlink → 위반
- [ ] `it.only` 추가 → test-integrity fail / 501줄 vs 500 제한 → limits fail
- [ ] allowed이면서 protected인 경로 → protected 우선

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 6. eval-engine — gate 실행 코어

### 목표
- phase 순서 강제·fail-fast·skipped·변수 보간·gate-report (EVAL_ENGINE_SPEC §3~§6).

### 구현 태스크
- [ ] `packages/eval-engine/src/interpolate.ts` — 5변수 치환, 잔존 `${...}` 에러
- [ ] `src/gate-executor.ts` — shared runCommand 실행, `logs/gates/<name>.{stdout,stderr}.log`, exit 0→pass/≠0→fail/timeout·spawn 실패→error
- [ ] `src/builtin.ts` — `builtin:` 디스패처(Phase 5 가드 5종 → GateResult 어댑트)
- [ ] `src/orchestrator.ts` — builtin guards → 프로젝트 명령 → advisory 그룹 실행, required guard 실패 시 이후 전부 `skipped`, 프로젝트 명령 phase 내 required 실패 시 잔여 skipped (fail-fast 고정)
- [ ] `src/gate-report.ts` — `gate-report.json` (§5 계약)

### 자체 테스트
- [ ] guard 실패 시 프로젝트 명령 프로세스 spawn 0회 (마커 파일 검증)
- [ ] pass/fail/error/skipped 합 = 게이트 수 (누락 없음)
- [ ] timeout 게이트 status 'error' + 잔존 프로세스 없음
- [ ] `${WORKTREE_ROOT}` 치환 실행 확인

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 7. eval-engine — baseline·test-on-base·evidence detector

### 목표
- 개선 증거 측정 메커니즘 (EVAL_ENGINE_SPEC §7). base-pass 게이밍 차단.

### 구현 태스크
- [ ] `src/baseline.ts` — agent 실행 전 clean worktree에서 비교형 게이트 실행 → `metrics/baseline.json`, `(projectId, baseCommit, evalConfigHash)` 키 캐시, base red 테스트 목록 기록
- [ ] `src/test-on-base.ts` — candidate.patch 테스트 변경만 분리(applyPatch includeOnly) → base 적용 → required_tests 실행(기대 fail) → candidate 동일 테스트(기대 pass) → `reports/test-on-base.json`
- [ ] `src/detectors/` 6종 — fixes_reproduced_failure / adds_regression_test / increases_coverage / improves_latency / reduces_security_risk / removes_duplicate_code, 각각 present|missing|inconclusive + artifact_ref
- [ ] `src/evidence.ts` — required_evidence 집계 (전부 missing vs 일부 inconclusive 구분)

### 자체 테스트
- [ ] base fail→candidate pass 테스트만 adds_regression_test present
- [ ] base에서도 pass하는 신규 테스트 → missing
- [ ] baseline 캐시 2회째 게이트 미실행 / baseline 부재 latency 주장 → inconclusive
- [ ] coverage 70→72 → increases_coverage present

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 8. decision engine·eval-report

### 목표
- first-match-wins 12규칙 순수 함수 + 스키마 검증된 report (EVAL_ENGINE_SPEC §8).

### 구현 태스크
- [ ] `src/decision/rules.ts` — reason code 상수(`NO_CHANGED_FILES`…`ALL_PASS`) + 12규칙 데이터 정의
- [ ] `src/decision/engine.ts` — `decide(input)` → `{decision, reasons:[{code,message,ref?}]}`, 부수효과 없음
- [ ] `src/report/eval-report.ts` — report 조립 → eval-report.schema 검증 통과 후 기록 (실패 시 throw)
- [ ] `src/report/summary.ts` — 사람용 한 단락 summary

### 자체 테스트
- [ ] 12규칙 각각 기대 decision·code 일치 (rule 3 vs 4 meta-eval 분기 포함)
- [ ] scope 위반 + required gate fail 동시 → 우선순위 5 적용
- [ ] 동일 입력 100회 → 동일 출력 (결정성)
- [ ] guard 실패 입력에서도 report 생성 + 스키마 통과 (`improvement_evidence` 빈 배열 포함)

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 9. agent adapter·LLM proxy

### 목표
- mock/codex adapter + localhost reverse proxy (SECURITY_MODEL §5.1). mock은 fixture 구동 엔진.

### 구현 태스크
- [ ] `packages/agent-adapters/src/adapter.ts` — `run({worktree, taskFile, env, timeoutMs})` 인터페이스
- [ ] `src/mock.ts` — 시나리오 JSON(파일 생성/수정/삭제/commit/.git 변조) worktree 적용
- [ ] `src/codex.ts` — Codex CLI 명령 조립, scrubbed env + `OPENAI_BASE_URL` 주입, timeout=limits.agent_timeout_seconds
- [ ] `src/proxy/server.ts` — localhost proxy: key 부착·loop별 토큰 집계
- [ ] `src/proxy/redact.ts` — proxy 로그 Authorization/key 마스킹

### 자체 테스트
- [ ] mock 시나리오 {modify, create, commit} → extractDiff로 전부 검출
- [ ] codex env에 실제 key 부재 + BASE_URL만 존재
- [ ] mock upstream으로 proxy 경유 → upstream엔 key 도달, proxy 로그엔 key 0회
- [ ] timeout 시나리오 kill + error

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 10. CLI — 커널 16단계 배선

### 목표
- `vibeloop run` 한 번에 ARCHITECTURE §3 16단계. 모든 경로에서 eval-report 생성.

### 구현 태스크
- [ ] `packages/cli/src/run.ts` — validate → base resolve → worktree → deps → baseline → git snapshot → agent → git integrity → diff → guards → test-on-base → gates → evidence → advisory → decision → report 배선, 단계를 LOOP_STATE_MACHINE 상태명으로 구조화 로그
- [ ] 입력 고정 기록 — `input/{task.yaml, eval.yaml, base_commit.txt, env-snapshot.json}` + `workspace/workspace-ref.json` (재현성 기준의 전제)
- [ ] `src/commands/run.ts` — `--repo --task --eval --agent --out`, `--agent mock:<scenario.json>` 지원
- [ ] `src/commands/retry.ts` — `vibeloop retry <loop-id> --mode retry_same_base|retry_latest_base|retry_eval_only|retry_critic_only` (새 loop-id, eval_only는 보관된 candidate.patch를 applyPatch로 재적용)
- [ ] `src/commands/report.ts`(콘솔 표) + `src/commands/gc.ts`(`--dry-run` 기본)
- [ ] SIGINT 1회 graceful 취소(workspace cleanup + cancelled 기록)·2회 즉시 종료 + `src/exit-codes.ts`(accept=0, reject=10, needs_human_review=11, needs_more_tests=12, cancelled=20, failed=2)

### 자체 테스트
- [ ] mock happy-path → eval-report.json 생성 + exit 0
- [ ] guard fail 경로 → exit 10 + report 존재 + 프로젝트 게이트 skipped
- [ ] `retry --mode retry_eval_only` → 새 loop-id, agent 미실행으로 동일 patch 재평가
- [ ] SIGINT → exit 20 + `git worktree list` 잔존 없음
- [ ] input/ 4파일 + workspace-ref.json 존재

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 11. fixture e2e — MVP-0 완료 게이트

### 목표
- MVP_IMPLEMENTATION_PLAN §7 fixture 16종 전부 자동화 + 재현성 검증.

### 구현 태스크
- [ ] `tests/e2e/fixtures/target-repo/` — 소형 npm 프로젝트 템플릿(vitest 테스트 포함), 헬퍼가 임시 복제 + git init
- [ ] guard 계열 6종 — scope 밖/untracked scope 밖/symlink/`.git` 변조/eval.yaml 수정/limits 초과 → reject + reason code assert
- [ ] integrity·evidence 계열 5종 — test.skip/assertion 삭제/no changed files/base-pass 테스트만 추가/evidence 없는 변경
- [ ] risk·운영 계열 5종 — auth 경로(needs_human_review)/자체 commit/low-risk 정상(accept)/게이트 timeout/meta-eval 분기
- [ ] `tests/e2e/run-all.e2e.test.ts` — 16종 × (decision, reason code, report 스키마, skipped 정합) 표 기반 assert + 동일 입력 2회 동일 결과(재현성)

### 자체 테스트
- [ ] `pnpm test:e2e` 16/16 기대 decision 일치
- [ ] 전 fixture report가 eval-report.schema 검증 통과
- [ ] 임시 디렉터리 누수 0 / 전체 5분 이내(deps 캐시 적중 기준)

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능 (= MVP-0 완료 선언)

## Phase 12. 정적 HTML report·retention 운영 (MVP-1)

### 목표
- 서버 없이 보는 self-contained report + gc 실동작.

### 구현 태스크
- [ ] `packages/report-html/src/render.ts` — eval-report + gate-report → 단일 HTML(gate 표·evidence·reasons·changed files·로그 링크)
- [ ] `src/template.ts` — 외부 요청 0건 인라인 CSS
- [ ] `vibeloop report <loop-id> --html [--open]` 연결
- [ ] `vibeloop gc --apply` — 만료 run 삭제 + manifest/삭제 기록 보존

### 자체 테스트
- [ ] accept/reject report 각각 HTML 렌더, file:// 열람 가능(네트워크 요청 0)
- [ ] 만료 전 run은 gc가 보존, 만료 run만 삭제
- [ ] 실제 Codex CLI 1회 수동 검증: low-risk patch accept + proxy 토큰 집계 확인 (MVP-1 완료 조건)

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 13. DB·API 서버 (MVP-2 기반)

### 목표
- DB_SCHEMA 13모델 + API_SPEC 전 엔드포인트. 커널은 라이브러리 호출.

### 구현 태스크
- [ ] `prisma/schema.prisma` — DB_SCHEMA.md 모델 전사: 이 Phase에서는 13개(LoopEvent `@@unique([loopRunId, seq])`, LoopRun.requestHash 포함), MVP-4 전용 `ImprovementCandidate`는 Phase 16 migration으로 추가 + 초기 migration + `docker-compose.yml`(postgres)
- [ ] `apps/server/src/app.ts` + `src/auth.ts` — Fastify 부트스트랩, Bearer 단일 토큰, 표준 에러 포맷
- [ ] `src/routes/{projects,tasks}.ts` — CRUD + `POST /tasks/:id/validate`(task-protocol 재사용)
- [ ] `src/routes/loops.ts` — `POST /loops`: Idempotency-Key + requestHash(정규화 sha256)로 replay/409 분기, **같은 task active loop 1개 제한**, in-process 큐로 runLoop, cancel + retry 4모드(새 loop)
- [ ] `src/routes/events.ts` — 상태 전이마다 LoopEvent(seq) 영속 → SSE, `Last-Event-ID` 이후 DB 재전송
- [ ] `src/routes/approvals.ts`(approve/reject/request-more-tests, needs_human_review 전이 규칙 검증) + `src/routes/artifacts.ts`(realpath로 root 밖 404)

### 자체 테스트
- [ ] `prisma validate` + migrate 성공
- [ ] 동일 key+동일 본문 → 같은 loop / 다른 본문 → 409 / active loop 존재 시 신규 생성 거부
- [ ] SSE 재연결 시 Last-Event-ID 이후만 수신(누락·중복 0) / seq 단조 증가
- [ ] 무토큰 401 / `GET /artifacts/../../etc/passwd` → 404 / 비대상 loop approve → 409

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 14. 웹 report viewer (MVP-2)

### 목표
- "실행을 보여주는" 웹. 실행 트리거는 CLI 유지.

### 구현 태스크
- [ ] `apps/web` Next.js(App Router) + `lib/api.ts`(토큰 env)
- [ ] `app/projects/page.tsx` + `app/projects/[id]/page.tsx` — 프로젝트/태스크/루프 목록(status·decision 뱃지)
- [ ] `app/loops/[id]/page.tsx` — gate 상태 표·로그 뷰어·SSE 구독(seq 기준 dedup, Last-Event-ID 재연결)
- [ ] `app/loops/[id]/report/page.tsx` — decision reasons(code+message)·evidence(artifact 링크)·changed files
- [ ] `app/approvals/page.tsx` — approve/reject/request-more-tests + decision_reason 입력
- [ ] Playwright smoke — 목록→상세→report→승인 1회

### 자체 테스트
- [ ] smoke 통과 (happy loop report에 ALL_PASS 표시)
- [ ] 네트워크 단절 후 SSE 재연결 시 로그 중복 0
- [ ] 미존재 loop → 404 / `pnpm --filter web build` 성공

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 15. GitHub Draft PR (MVP-3)

### 목표
- accepted/approved 한정 draft PR. token은 서버 측 전용 (SECURITY_MODEL §9).

### 구현 태스크
- [ ] `packages/github-integration/src/branch.ts` — safeGit으로 base에서 branch 생성 → candidate.patch applyPatch → commit → push(fine-grained token, agent 미전달)
- [ ] `src/pull-request.ts` — draft PR 생성 + body에 eval-report summary(decision·reasons·gate 통계) 마크다운
- [ ] PullRequest lifecycle — `creating → draft_created | create_failed`, 재시도 멱등(기존 branch/PR 탐지, 중복 PR 금지)
- [ ] `POST /api/loops/:id/pull-request` — accepted/approved 외 전부 403(needs_more_tests·미승인 needs_human_review 포함)
- [ ] GitHub API mock(nock) 통합 테스트 — 성공/실패/재시도/조건 위반

### 자체 테스트
- [ ] accepted loop → draft PR + body에 reason code 포함
- [ ] 금지 상태 5종 전부 403
- [ ] push 실패 → create_failed → 재시도 시 PR 정확히 1개

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 16. Improvement Discovery·candidate 큐 (MVP-4)

### 목표
- 봇이 스스로 문제를 발견해 candidate 큐에 쌓고, deterministic 규칙으로 task.yaml을 자동 생성한다. 발견 입력은 untrusted로 취급한다 (AUTONOMOUS_LOOP_SPEC §3~§4).

### 구현 태스크
- [ ] `packages/discovery/src/collectors/` — test/typecheck/lint/security-scan 수집기 4종: 커널 gate executor로 명령 실행 후 결과 파싱 → 구조화 candidate(경로·테스트명·에러 코드만, **원문 텍스트 비포함**)
- [ ] `src/fingerprint.ts` — `sha256(source + 정규화 위치 + 유형)` + dedup (기존·dismissed fingerprint 재제안 차단)
- [ ] `src/task-gen.ts` — candidate → task.yaml deterministic 변환: 실패 위치 기반 최소 write_scope, risk 보수 분류(불명 → unknown), source별 required_evidence 기본값, 전역 limits 그대로(완화 금지), task.schema 검증 통과 필수
- [ ] `prisma`에 `ImprovementCandidate` migration + `apps/server/src/routes/candidates.ts` — 목록/수동 등록/approve/dismiss/`discovery/run` (API_SPEC §10)
- [ ] `app/candidates/page.tsx` — 후보 목록 + approve/dismiss 승인 화면 (supervised 모드의 관문)
- [ ] CLI `vibeloop discover --repo` — 저장 없이 후보 출력 (로컬 점검용 dry-run)

### 자체 테스트
- [ ] 실패 테스트 1개 심은 fixture repo → candidate 정확히 1개 생성
- [ ] 동일 상태 재실행 → 중복 candidate 0 (fingerprint) / dismissed 후 재실행 → 재제안 0
- [ ] 자동 생성 task.yaml이 schema 검증 통과 + write_scope가 실패 파일 경로로 제한됨
- [ ] 로그 원문에 악성 지시문("ignore previous instructions...") 주입 → candidate 구조화 필드에 원문 미포함, task objective는 하네스 정형 문장만
- [ ] proposed 50개 초과 생성 시 우선순위 상위만 유지

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 17. Loop Orchestrator·guardrails (MVP-4)

### 목표
- 발견 → (승인) → 1개씩 순차 루프 → 통과 시 PR → 다음 후보 반복을 무인으로. 모든 guardrail의 기본값은 안전값 (AUTONOMOUS_LOOP_SPEC §5~§6).

### 구현 태스크
- [ ] `apps/server/src/orchestrator/scheduler.ts` — 프로젝트당 1개, 우선순위순 candidate 1개 선택 → runLoop 실행 → 종료 처리(accept→PR 생성, needs_human_review→승인 큐 대기, rejected→재시도 정책) → 다음 후보. 매 루프 전 default branch fetch로 latest base 갱신
- [ ] candidate 상태 머신 전이 구현 — `proposed→approved→queued→running→processed|dismissed` (supervised: 사람 승인 / auto: low-risk 자동 승인, 위험 영역은 커널 needs_human_review 그대로)
- [ ] `src/orchestrator/guardrails.ts` — 일일 루프 예산(기본 20), 토큰 예산(proxy 집계 연동, 설정 필수), 동일 candidate 재시도 2회→dismissed+알림, 연속 실패 5회→자동 정지, open draft PR 상한 5, 발견 주기(기본 30분)
- [ ] kill switch — `orchestrator/stop` API: 실행 중 루프 graceful cancel 후 즉시 정지 (모든 상태에서 최우선)
- [ ] 상태 영속·복구 — 모드/예산 사용량/현재 candidate 저장, 재시작 시 running 좀비 → failed 처리 후 재큐
- [ ] `app/orchestrator/page.tsx` — 모드·예산 사용량·큐 길이·최근 이력 + start(supervised|auto)/stop 버튼

### 자체 테스트
- [ ] 가짜 후보 2개 → supervised 승인 → **순차** 처리(동시 실행 0) → draft PR 2개(mock) 생성 → 큐 소진 후 발견 주기 대기
- [ ] kill switch → 실행 중 루프 graceful cancel + worktree 잔존 없음 + 즉시 정지
- [ ] 동일 후보 2회 reject → dismissed + `candidate.dismissed.retry_limit` 이벤트
- [ ] 연속 실패 5회 → `orchestrator.paused` 자동 정지 / 일일 예산 초과 → 일시정지
- [ ] open draft PR 5개 상태에서 새 PR 루프 중단
- [ ] 재시작 시 멈춘 지점 복구 (running 좀비 → failed 후 재큐)

### 이슈 및 수정
- [ ] 발견 이슈 없음

### 완료 조건
- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 전체 워크스트림 완료 (= "발견→수정→채점→PR→반복" 전체 그림 가동)

## 잔여 리스크 / 후속 과제

- LLM critic runner (advisory 게이트의 LLM 구현체) — 본 계획에서는 advisory 명령 게이트 실행 경로까지만.
- `vibeloop init` — 대상 repo에 eval.yaml/eval.sh/ACCEPTANCE.md 템플릿 생성기.
- 컨테이너 격리·OS 네트워크 차단 — MVP 신뢰 경계(SECURITY_MODEL §2)의 해소 수단, MVP 이후.
- flaky 프로젝트 테스트 → `retry_eval_only`(rejected 허용)로 운영 대응, gate 자동 retry는 결정성 보호를 위해 도입하지 않음.
- git 최소 버전(≥2.31) 사전 체크는 Phase 1에서, CI 매트릭스는 ubuntu+macos.
- discovery 소스 확장(CI 로그·이슈 트래커·에러 모니터링)과 LLM 기반 candidate 고도화(원인 추정·중복 군집화)는 MVP-4 가동 데이터를 보고 후속 결정.
- learnings/SKILL.md 자동 반영(Skill Manager)은 MVP-5 — 자율 루프의 reject 사유 축적이 입력이 된다.
