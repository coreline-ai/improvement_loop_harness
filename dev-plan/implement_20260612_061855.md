# implement_20260612_061855.md

작성 일시: `2026-06-12 06:18:55 KST`

이 문서는 이번 개발의 범위를 고정하고, **동일/유사 LLM이 코드 수정·리뷰·테스트 판단을 모두 수행할 때 통과 의미가 희석되는 문제**를 방지하기 위한 보강 작업 문서다.
프로젝트의 원래 목적은 유지한다: **봇이 문제를 계속 발견하고, 한 번에 1개씩 수정하며, 고정된 통과 기준을 만족한 변경만 draft PR로 만들고, 다시 반복한다.**

## 개발 목적

- LLM은 **수정자/제안자** 역할로 제한하고, 최종 통과 판정은 deterministic harness가 수행하도록 신뢰 경계를 강화한다.
- `eval-report.json`의 `decision: accept`가 단순한 “LLM이 괜찮다고 판단함”이 아니라, 독립 검증·증거·재현 가능한 게이트를 통과했다는 의미가 되도록 만든다.
- prompt injection, self-preference bias, overreliance, test gaming, eval 파일 변조, PR 스팸을 명시적으로 방어한다.
- 자동 루프의 산출물은 **auto-merge가 아니라 draft PR**이며, 위험 변경은 human review로 라우팅한다.

## 개발 범위

- 통과 기준 신뢰 경계 강화
  - LLM critic/adversarial review는 advisory로만 유지
  - final decision은 deterministic decision engine만 수행
  - `eval-report.json` 생성·서명·검증 경로를 agent 수정 권한 밖으로 분리
- 증거 기반 검증 강화
  - fail-to-pass / pass-to-pass 테스트 묶음 분리
  - hidden/held-out acceptance test 운용 방식 문서화 및 구현 준비
  - evidence artifact provenance/hash 기록
- 독립 검증 레인 추가
  - 로컬 runner 결과와 별도 CI/verifier 결과를 비교
  - 동일 LLM self-review 결과를 최종 authority로 사용하지 않도록 정책화
- prompt injection 방어 강화
  - issue/log/doc/comment 등 자동 발견 입력의 raw text 격리
  - candidate/task 변환 시 구조화 추출 + 최소 write_scope 적용
- 리포트/웹/API 표시 강화
  - 어떤 검증이 deterministic인지, 어떤 검증이 advisory인지 명확히 표기
  - human review 필요 사유를 reason code로 고정

## 제외 범위

- 자동 merge 구현은 제외한다. 모든 자동 산출물은 draft PR까지만 허용한다.
- LLM judge를 최종 합격 판정자로 승격하지 않는다.
- target repository의 테스트 파일을 신뢰 기준 자체로 삼지 않는다. 테스트 변경은 별도 integrity 검증 대상이다.
- 모든 외부 SaaS verifier 연동을 즉시 구현하지 않는다. MVP는 로컬/CI 독립 검증 계약과 확장 포인트를 우선 만든다.
- 대규모 agent framework 교체나 모델 벤더 종속 기능 추가는 제외한다.

## 참조 문서

### 내부 문서

- [기존 개발 계획](./implement_20260610_223129.md)
- [자율 루프 명세](../docs/AUTONOMOUS_LOOP_SPEC.md)
- [Eval Engine 명세](../docs/EVAL_ENGINE_SPEC.md)
- [Security Model](../docs/SECURITY_MODEL.md)
- [Task Protocol](../docs/TASK_PROTOCOL.md)
- [Loop State Machine](../docs/LOOP_STATE_MACHINE.md)
- [API Spec](../docs/API_SPEC.md)
- [DB Schema](../docs/DB_SCHEMA.md)

### 외부 근거

- OWASP Top 10 for LLM Applications — prompt injection, excessive agency, overreliance 위협 모델
  https://owasp.org/www-project-top-10-for-large-language-model-applications/
- OWASP Prompt Injection Prevention Cheat Sheet — direct/indirect prompt injection, untrusted content 처리 원칙
  https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html
- SWE-bench — real issue 기반 fail-to-pass 테스트 평가 관점
  https://www.swebench.com/original.html
- OpenAI SWE-bench Verified — FAIL_TO_PASS/PASS_TO_PASS 및 agent에 테스트를 숨기는 검증 관점
  https://openai.com/index/introducing-swe-bench-verified/
- OpenAI Evals guide — model grader는 오류율이 있으므로 human/independent validation 필요
  https://developers.openai.com/cookbook/examples/evaluation/getting_started_with_openai_evals
- Self-Preference Bias in LLM-as-a-Judge — LLM judge의 자기 선호/편향 위험
  https://openreview.net/forum?id=Ns8zGZ0lmM

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- `eval.yaml`, `task.yaml`, schema, protected paths, decision rules 변경은 반드시 문서와 테스트를 동시에 갱신한다.
- LLM 출력은 근거 자료일 수 있지만, 통과 판정의 최종 근거가 될 수 없다.
- 같은 LLM/같은 prompt 계열의 reviewer 결과는 advisory evidence로만 기록한다.

## 고정 통과 의미

| 항목 | 통과 의미 | 최종 authority |
|---|---|---|
| 수정 생성 | Agent/LLM이 후보 patch 생성 | 아님 |
| 비판/적대적 리뷰 | LLM critic 또는 static reviewer가 문제 제기 | 아님, advisory |
| 테스트 실행 | 지정 gate command 결과 수집 | 부분 authority |
| 증거 검증 | 신규 테스트·보안 개선·성능 개선 evidence 탐지 | deterministic detector |
| 보호 경로 검증 | eval/schema/guard/protected file 변조 차단 | builtin guard |
| 최종 판정 | `decision: accept`, `reason: ALL_PASS` 산출 | deterministic decision engine |
| PR 생성 | accepted 또는 human approved 후보만 draft PR 생성 | orchestrator policy |

최종 `accept` 조건은 아래 조합을 모두 만족해야 한다.

```text
builtin guards pass
+ required gates pass
+ required evidence present
+ fail-to-pass evidence 검증
+ pass-to-pass regression 유지
+ protected path/write_scope 위반 없음
+ risk low/known 또는 human approval
+ eval-report schema/provenance 검증
= decision: accept / ALL_PASS
```

## Phase 상태 요약

- [ ] Phase 1 완료 — 신뢰 경계/정책 문서 정합화
- [ ] Phase 2 완료 — eval artifact provenance 및 변조 방지 강화
- [ ] Phase 3 완료 — hidden/held-out 및 pass-to-pass 검증 계약 추가
- [ ] Phase 4 완료 — 독립 verifier/CI 레인 계약 추가
- [ ] Phase 5 완료 — prompt injection 방어형 candidate intake 강화
- [ ] Phase 6 완료 — LLM critic/adversarial review 격리 및 편향 방지
- [ ] Phase 7 완료 — API/UI/report 표시 및 회귀 테스트

## Phase 1. 신뢰 경계/정책 문서 정합화

### 목표

- 현재 명세에 이미 존재하는 보호장치를 한 곳에서 읽히도록 정리한다.
- “LLM이 수정하고 LLM이 통과시킨다”는 오해가 생기지 않도록 역할과 authority를 명확히 한다.

### 구현 태스크

- [ ] `SECURITY_MODEL.md`에 `LLM self-review dilution` 위협 항목 추가
- [ ] `EVAL_ENGINE_SPEC.md`에 final authority 표 추가
- [ ] `AUTONOMOUS_LOOP_SPEC.md`에 draft PR 생성 전 필수 accept 조건 재명시
- [ ] `TASK_PROTOCOL.md`에 LLM이 수정 가능한 파일과 수정 불가 artifact 범위 명시
- [ ] `docs/README.md`에서 trust-boundary 관련 문서 링크 정리

### 자체 테스트

- [ ] `rg "LLM critic.*advisory|decision engine|ALL_PASS" docs`로 문서 정합성 확인
- [ ] `git diff --check` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 2. eval artifact provenance 및 변조 방지 강화

### 목표

- agent가 만든 patch와 harness가 만든 판정 artifact를 분리한다.
- `eval-report.json`이 누가/어떤 입력/어떤 gate 결과로 생성됐는지 재현 가능하게 한다.

### 구현 태스크

- [ ] `eval-report.json` schema에 provenance 필드 추가 검토
  - [ ] `harness_version`
  - [ ] `decision_engine_version`
  - [ ] `task_hash`
  - [ ] `eval_config_hash`
  - [ ] `candidate_patch_hash`
  - [ ] `gate_artifact_hashes`
  - [ ] `generated_by: harness`
- [ ] agent workspace에서 `eval-report.json` 직접 작성/수정 시 reject하는 guard 추가 또는 기존 guard 강화
- [ ] protected paths 기본값에 schema/eval/task/guard 관련 파일 누락 여부 점검
- [ ] artifact hash mismatch 시 `reject / ARTIFACT_PROVENANCE_MISMATCH` reason code 추가 검토
- [ ] 기존 fixture에 provenance 성공/실패 케이스 추가

### 자체 테스트

- [ ] provenance 정상 fixture 통과
- [ ] agent가 `eval-report.json`을 변경하는 fixture reject
- [ ] artifact hash가 바뀐 fixture reject
- [ ] `pnpm test` 통과
- [ ] `pnpm test:e2e` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 3. hidden/held-out 및 pass-to-pass 검증 계약 추가

### 목표

- agent가 보이는 테스트만 맞추는 test gaming을 줄인다.
- 신규 기능/버그 수정은 fail-to-pass로 증명하고, 기존 동작은 pass-to-pass로 보존한다.

### 구현 태스크

- [ ] `eval.yaml`에 test group 개념 추가 검토
  - [ ] `fail_to_pass`
  - [ ] `pass_to_pass`
  - [ ] `hidden_acceptance`
  - [ ] `regression`
- [ ] hidden test는 agent prompt/context에 노출하지 않는 운영 규칙 문서화
- [ ] base workspace에서는 `fail_to_pass` 실패, candidate workspace에서는 통과해야 evidence 인정
- [ ] `pass_to_pass`는 base/candidate 양쪽 모두 통과해야 regression 유지로 인정
- [ ] hidden test 실패 시 `reject / HIDDEN_ACCEPTANCE_FAILED` reason code 추가 검토
- [ ] test group별 결과를 `gate-report.json`과 `eval-report.json`에 연결

### 자체 테스트

- [ ] fail-to-pass 성공 fixture 통과
- [ ] base에서도 pass하는 신규 테스트는 evidence 불인정
- [ ] pass-to-pass가 candidate에서 깨지면 reject
- [ ] hidden_acceptance 실패 fixture reject
- [ ] `pnpm test:e2e` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 4. 독립 verifier/CI 레인 계약 추가

### 목표

- 같은 실행 환경/같은 agent 컨텍스트의 self-check만으로 PR을 만들지 않도록 한다.
- 최소 1개의 독립 검증 레인을 draft PR 생성 전 조건으로 둘 수 있게 한다.

### 구현 태스크

- [ ] `VerifierRun` 또는 기존 `GateRun` 확장으로 verifier lane 결과 저장 방식 결정
- [ ] verifier 종류 정의
  - [ ] local harness
  - [ ] CI harness
  - [ ] external verifier placeholder
- [ ] `accept` 전 필수 verifier 정책 추가 검토
  - [ ] MVP 기본값: local harness required
  - [ ] strict 모드: CI verifier required
- [ ] GitHub Actions workflow 초안 추가 검토
  - [ ] typecheck
  - [ ] lint
  - [ ] unit
  - [ ] e2e
  - [ ] smoke
- [ ] verifier mismatch 시 `needs_human_review / VERIFIER_MISMATCH`로 라우팅

### 자체 테스트

- [ ] local verifier pass fixture 통과
- [ ] verifier mismatch fixture는 human review 필요
- [ ] CI workflow yaml lint 또는 dry-run 가능한 범위 확인
- [ ] `pnpm typecheck && pnpm lint && pnpm test && pnpm test:e2e` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 5. prompt injection 방어형 candidate intake 강화

### 목표

- issue/log/doc/comment 등 외부 입력이 agent system prompt나 eval 정책을 우회하지 못하게 한다.
- 자동 발견된 문제는 구조화된 candidate로 변환한 뒤 최소 권한으로 처리한다.

### 구현 태스크

- [ ] raw discovery input 저장 위치와 agent 전달 위치 분리
- [ ] candidate 생성 시 raw text를 그대로 prompt에 주입하지 않고 구조화 필드만 사용
- [ ] candidate 필드 allowlist 정의
  - [ ] title
  - [ ] symptom
  - [ ] repro command
  - [ ] suspected files
  - [ ] write_scope
  - [ ] risk_area
- [ ] prompt injection indicator 탐지 시 `needs_human_review`로 라우팅
- [ ] `write_scope` 자동 축소 규칙 추가 검토
- [ ] discovery source별 trust level 기록

### 자체 테스트

- [ ] malicious issue body fixture가 agent instruction으로 승격되지 않음
- [ ] raw text 내 “ignore previous instructions”류 문구가 task policy를 변경하지 못함
- [ ] suspicious candidate는 human review로 라우팅
- [ ] `pnpm test` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 6. LLM critic/adversarial review 격리 및 편향 방지

### 목표

- LLM 기반 적대적 리뷰의 가치는 유지하되, 자기 선호/동조 편향이 최종 통과 판정으로 이어지지 않게 한다.

### 구현 태스크

- [ ] LLM critic 출력 schema를 advisory finding으로 고정
- [ ] critic 결과가 decision rule에 직접 참여하지 못하도록 테스트 추가
- [ ] critic 모델/agent 모델이 같을 경우 report에 `same_model_review: true` 표시
- [ ] strict 모드에서 수정 agent와 다른 reviewer model/provider 요구 옵션 검토
- [ ] critic이 추가 테스트를 요구하면 자동 accept가 아니라 `needs_human_review` 또는 추가 deterministic gate 요구로 변환
- [ ] critic prompt에 raw untrusted input이 직접 들어가지 않도록 intake 정책과 연결

### 자체 테스트

- [ ] critic이 “approve”라고 해도 deterministic gate 실패 시 reject
- [ ] critic이 “reject”라고 해도 required deterministic gate 통과 시 advisory warning으로만 기록 또는 정책대로 human review
- [ ] same model critic 사용 시 report에 표시
- [ ] `pnpm test` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 다음 Phase 진행 가능

## Phase 7. API/UI/report 표시 및 회귀 테스트

### 목표

- 사용자가 report 화면에서 “무엇이 진짜 통과 근거인지” 즉시 이해하게 한다.
- 운영자가 PR 생성 전 신뢰 수준을 확인할 수 있게 한다.

### 구현 태스크

- [ ] API response에 trust boundary 요약 필드 추가 검토
- [ ] 웹 report viewer에 다음 구분 표시
  - [ ] deterministic pass/fail
  - [ ] advisory findings
  - [ ] provenance verified
  - [ ] hidden/held-out status
  - [ ] verifier lane status
  - [ ] human review required reason
- [ ] draft PR body에 accept 근거 요약 추가
- [ ] orchestrator event에 guardrail/provenance/verifier 이벤트 추가
- [ ] smoke 테스트에 report 표시 확인 추가

### 자체 테스트

- [ ] report viewer smoke 통과
- [ ] PR body snapshot 또는 unit test 통과
- [ ] `pnpm --filter web test:smoke` 통과
- [ ] `pnpm build` 통과
- [ ] `git diff --check` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] 개발 문서 기준 전체 Phase 완료 가능

## 최종 완료 조건

- [ ] `eval-report.json`은 harness만 생성하며 agent patch가 직접 생성/수정할 수 없다.
- [ ] LLM critic은 advisory로만 기록되고 final decision authority가 아니다.
- [ ] fail-to-pass와 pass-to-pass 검증 의미가 명세·schema·테스트에 반영된다.
- [ ] hidden/held-out test 운영 계약이 문서화되고, 실패 시 자동 accept가 불가능하다.
- [ ] prompt injection 의심 candidate는 자동 수정/PR 루프로 바로 들어가지 않는다.
- [ ] 독립 verifier lane 결과가 report/API/UI 또는 PR body에 표시된다.
- [ ] `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm test:e2e`, `pnpm build`, web smoke가 통과한다.

## 잔여 리스크 / 후속 과제

- hidden test는 운영상 보안 저장소/CI secret과 연결되어야 하므로, 로컬 개발 문서만으로 완전 해결되지 않는다.
- target repo 자체가 악성 스크립트를 포함할 수 있으므로 장기적으로 container/network sandbox가 필요하다.
- LLM judge를 완전히 배제해도 테스트 oracle 자체가 부실하면 accept 의미가 약해질 수 있다.
- strict verifier 모드는 비용과 속도를 증가시킬 수 있으므로 프로젝트별 policy preset이 필요하다.
