# implement_20260623_192314.md

작성 일시: `2026-06-23 19:23:14 Asia/Seoul`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

- 5개 병렬 적대 리뷰에서 나온 P0/P1/P2 항목을 제품 목적에 맞게 고정하고, PR 후보/Skill/release evidence 주장의 신뢰 경계를 강화한다.

## 개발 범위

- P0: `skip-final-reverify`가 PR 후보 또는 promotion으로 오인되지 않도록 SDK/CLI predicate와 출력 계약을 강화한다.
- P0: Server draft PR route가 SDK/CLI와 같은 trust floor를 강제하도록 final verification/provenance/patch hash/quality evidence를 검사한다.
- P1: GitHub draft PR 재사용 경로가 stale/non-draft/auto-merge PR을 성공으로 보지 않게 검증한다.
- P1: PR body/evidence 문구에서 candidate patch hash와 GitHub patch email format hash를 혼동하지 않게 한다.
- P1: clean `CODEX_HOME/skills` 블랙박스 UAT와 hidden runtime self-inspection 방어는 release blocker로 추적하고 최소 failing/coverage gap을 문서화한다.
- P2: Postgres capable release preflight 재실행과 proven/unproven 문서 정리는 별도 운영 검증 phase로 둔다.

## 제외 범위

- 임의/대형 사용자 repo 전체 100% PASS 선언.
- 새 semantic/M4 corpus 확장.
- GitHub repo 삭제 자동화.
- PR 자동 merge.
- Skill marketplace 배포 자체.

## 참조 문서

- `README.md`
- `AGENTS.md`
- `docs/RELEASE_GATE_MATRIX.md`
- `docs/SELF_IMPROVEMENT_LOOP_RUNBOOK.md`
- `skills/vibeloop-harness/SKILL.md`
- 2026-06-23 병렬 적대 리뷰 결과: architecture 78, skill 68, safety 74, PR workflow 78, release evidence 74.

## 공통 진행 규칙

- 각 Phase는 자체 테스트를 포함한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- `main`에 있는 코드 기준으로 수정한다. 현재 열려 있는 PR #1의 selected-evidence patch는 아직 main에 병합되지 않았으므로 별도 변경으로 취급한다.
- 모든 PR 후보 주장은 `selected ∧ accept ∧ ALL_PASS ∧ qualified ∧ final_verification.reverified ∧ final_verification.passed` 기준으로만 허용한다.
- capability가 필요한 검증(Postgres/Docker/Codex/GitHub live)은 blocked/pass를 분리 기록한다.

## Phase 상태 요약

- [x] Phase 1 P0 skip-final-reverify trust floor 완료
- [x] Phase 2 P0 Server PR route trust floor 완료
- [x] Phase 3 P1 GitHub PR reuse/evidence wording 완료
- [x] Phase 4 P1 Skill black-box UAT/hidden runtime guard 정리 완료
- [x] Phase 5 P2 release preflight/docs truth 정리 완료

## Phase 1. P0 skip-final-reverify trust floor

### 목표

- final reverify를 실제 재실행하지 않은 결과가 PR 후보나 promotion 대상처럼 보이지 않게 한다.

### 구현 태스크

- [x] `isPrCandidate`가 `finalVerification.reverified === true`까지 요구하게 한다.
- [x] `skipFinalReverify` provenance-only 결과의 `passed` 의미를 PR predicate에서 배제한다.
- [x] `improve --promote-branch --skip-final-reverify`가 promotion을 만들지 않는 테스트를 추가한다. `orchestrate` 경로는 동일 predicate guard로 코드 배선했다.

### 자체 테스트

- [x] `corepack pnpm vitest run packages/sdk/src/pr-candidate.test.ts`
- [x] `corepack pnpm vitest run packages/cli/src/index.test.ts --testNamePattern "skipFinalReverify|skip-final-reverify|does not promote" --testTimeout 30000`

### 이슈 및 수정

- [x] stale built SDK 때문에 CLI targeted test가 첫 실행에서 실패했고, `corepack pnpm build` 후 재실행해 통과했다.

### 완료 조건

- [x] `skip-final-reverify` 결과가 JSON에서 `pr_candidate=false`로 표시된다.
- [x] local branch promotion이 생성되지 않는다.
- [x] GitHub draft PR 차단 기존 테스트가 유지된다.

## Phase 2. P0 Server PR route trust floor

### 목표

- Server API의 PR 생성 경로가 SDK/CLI보다 약한 우회로가 되지 않게 한다.

### 구현 태스크

- [x] server route가 eval report/quality report/provenance/patch hash를 확인하고, `final_verification.passed=true` 및 `reverified=true`까지 요구한다.
- [x] `approved` 상태만으로 PR 생성이 허용되지 않도록 SDK single-run predicate(`accept ∧ ALL_PASS ∧ qualified`)와 provenance/hash evidence를 요구한다.
- [x] stale patch hash와 skipped final verification negative test를 추가했다. accepted-but-unqualified는 기존 orchestrator quality gate 테스트로 유지된다.

### 자체 테스트

- [x] `corepack pnpm vitest run apps/server/src/app.test.ts --testNamePattern "PR|pull request|draft PR|unqualified loop" --testTimeout 30000`
- [ ] 필요 시 `corepack pnpm vitest run apps/server/src/store-contract.test.ts`

### 완료 조건

- [x] Server PR route가 SDK PR predicate, quality report, provenance/hash binding, final verification reverified guard를 통과해야 PR 생성으로 진행한다.

## Phase 3. P1 GitHub PR reuse/evidence wording

### 목표

- 기존 PR/branch 재사용이 stale evidence를 성공으로 보지 않게 하고, hash 문구를 감사자가 오해하지 않게 한다.

### 구현 태스크

- [x] existing PR reuse 시 draft/base/head/body freshness를 검증한다.
- [x] auto-merge가 켜진 기존 PR은 실패 처리한다.
- [x] PR body의 hash 문구를 candidate patch artifact 기준으로 명확히 한다.

### 자체 테스트

- [x] `corepack pnpm vitest run packages/github-integration/src/github-integration.test.ts --testTimeout 30000`
- [x] `corepack pnpm vitest run packages/cli/src/index.test.ts --testNamePattern "draft PR" --testTimeout 30000`

### 완료 조건

- [x] reused PR이 stale/non-draft/auto-merge 상태면 통과하지 않는다.

## Phase 4. P1 Skill black-box UAT / hidden runtime self-inspection

### 목표

- Skill 제품성의 가장 큰 빈칸을 release blocker로 고정한다.

### 구현 태스크

- [x] clean `CODEX_HOME/skills`에 bundled skill만 설치하고 wrapper를 실행하는 블랙박스 smoke UAT를 추가했다.
- [x] hidden acceptance runtime self-inspection 공격 fixture와 deterministic guard를 추가했다.

### 자체 테스트

- [x] `corepack pnpm bundle:skill`
- [x] `corepack pnpm vitest run scripts/uat/skill-clean-codex-home-smoke.test.mjs --testTimeout 30000`
- [x] `corepack pnpm vitest run packages/guards/src/guards.test.ts --testNamePattern "hidden acceptance runtime|hidden runtime" --testTimeout 30000`
- [x] `corepack pnpm --filter @vibeloop/guards build && corepack pnpm vitest run packages/eval-engine/src/eval-engine.test.ts --testNamePattern "hidden-self-inspection" --testTimeout 30000`

### 완료 조건

- [x] clean `CODEX_HOME` smoke는 추가됐지만 전체 자연어 Skill UX full UAT는 별도 heavy/live lane으로 남는다.

## Phase 5. P2 release preflight / docs truth

### 목표

- release 완료 주장을 capable 환경 evidence와 맞춘다.

### 구현 태스크

- [x] Postgres capable 환경에서 `uat:release-gates-preflight` 재실행 완료.
- [x] proven/unproven 문구 충돌 정리.

### 자체 테스트

- [x] `corepack pnpm uat:postgres-contract:docker`
- [x] `TEST_DATABASE_URL='postgresql://vibeloop:vibeloop@127.0.0.1:54329/vibeloop' corepack pnpm uat:release-gates-preflight`
- [x] `corepack pnpm uat:release-evidence-audit`
- [x] `corepack pnpm build`
- [x] `git diff --check`

### 완료 조건

- [x] release gate complete/pass/blocked 표현이 evidence와 일치한다.
